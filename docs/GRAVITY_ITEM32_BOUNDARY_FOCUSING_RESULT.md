# Item 32 boundary focusing result

Date: **2026-08-28**

## Decision

`BOTH_FORMAL_TRACKS_NOT_PROMOTED_SCOPED_PARTIAL_PATTERN_RETAINED`

Neither the universal-gravity track nor the phenomenon/publication track is promoted. A
predeclared scoped-partial rule does retain the selected boundary-normal tensor and its exact
domain results for unchanged fresh replication. This is not evidence that gravity focuses at
baryonic boundaries and it is not a paper claim.

The immutable receipt is `runs/gravity/roadmap/item-32-boundary-focusing-v1.json`. Eight
confirmation galaxies remain sealed and their MAPS files were not downloaded. No paid API or
model call was made.

## Frozen real-data experiment

Item 32 inherited only Item 30's response-blind, predecessor-vetoed MaNGA/GEMA predictors. Before
roles, folds, maps, or kinematics it excluded all 1,000 Item 30 roles and all 200 Item 31 roles.
Frozen disk, signal-to-noise, Sérsic-index, and axis-ratio cuts left 52 fresh galaxies. Their median
stellar mass crossed with median Sérsic index produced four cells of exactly 13 objects each.

The committed role manifest selected 12 objects per cell:

- 40 exploration galaxies, ten per cell and eight per whole-galaxy outer fold;
- eight sealed confirmations, two per cell; and
- zero map pixels, response values, or object replacements used in role assignment.

All 40 exploration MAPS files were acquired and checksum-verified. Thirty-nine supplied the
minimum continuum source area; one small field failed without replacement. Exactly 30 galaxies,
or **75%** of the frozen exploration sample, pass both stellar annuli and therefore meet the
frozen 30-object/75% primary quality gate. Twenty-eight of the 39 source-complete galaxies pass
H-alpha quality, and 25 overlap the stellar-complete primary set used for unchanged transfer.

## Equal-capacity vector and tensor search

The grammar generated exactly **262,144** raw formulas, with **65,536** cells and matched polarity
in each niche:

1. **Baryonic-interface refraction** -- a known-family effective projection motivated by
   [refracted gravity](https://arxiv.org/abs/1603.04943), not its published field solution.
2. **Boundary-normal anisotropic tensor** -- a known-family combination of an anisotropic-medium
   tensor with continuum-light boundary normals; historical novelty is not claimed.
3. **Solenoidal boundary circulation** -- an explicitly nonconservative stress test that could not
   pass the universal track without an action or additional conserving degree of freedom.
4. **Hessian-ridge flux redirection** -- a potentially new synthesis using curvature anisotropy
   and principal-ridge direction; novelty and general non-equivalence remain unresolved.

For continuum flux `I`, each smoothing scale constructs a normalized boundary strength and local
direction field:

```text
B_sigma = |grad Gaussian_sigma(log(1 + I / median(I)))| / median_positive(|grad ...|)
chi = [B_sigma / (B_sigma + B_c)]^(sharpness * shape_power)
delta a_t = s A chi Q_n (r / r_mid)^gamma,
```

where `Q_n` is respectively the boundary-normal tangential component, `2 n_r n_t`, the rotated
normal tangential component, or `2 H_aniso p_r p_t`. Source-flux-weighted projection onto the
first angular harmonic gives `(delta c, delta s)` and the predicted axial direction

```text
theta_pred = wrap_180[atan2(delta s, 1 + delta c)].
```

The response is never an input to these fields. Finite, local, material, direction, and analytic
bend gates admit **169,876** cells: 42,594 interface, 42,424 tensor, 42,424 circulation, and 42,434
Hessian-ridge. Their exact parameter signatures collapse to **55,872** adversarial behavioral
classes; 114,004 duplicate behaviors are counted rather than advertised as independent ideas.

## Measured stellar result

All five whole-galaxy folds select the **enhancing boundary-normal anisotropic tensor**. Every fold
uses the maximum amplitude `A=0.5` and minimum boundary threshold `B_c=0.05`. Radial exponents and
smoothing scales vary, although one exact cell repeats in two folds. Neither the potentially new
Hessian-ridge synthesis nor the explicit circulation control is selected.

| Prediction | Held-out angular MSE (degrees squared) | Tensor relative result |
|---|---:|---:|
| Zero photometric/kinematic misalignment | 74.4775 | **9.04% better** |
| Structural ridge control | 87.3023 | **22.40% better** |
| Flexible structure + continuum-boundary model | 128.601 | **47.32% better** |
| Selected tensor | 67.7449 | — |

The apparent improvements do not survive the guarded full-search test: the 99-trial
selection-aware permutation result is **`p=0.67`**, and the largest null improvement over the
flexible model is 85.20%. Thirteen of 30 galaxies are individual counterexamples relative to the
flexible model.

Both stellar-mass halves improve over the flexible baseline, but the high-mass half is **10.34%
worse** than zero alignment. The high-Sérsic half is 7.03% worse than flexible. Thus the frozen
all-broad-slice gate fails even though seven individual predeclared halves clear the scoped 5%
threshold against flexible. The three-object low-boundary-strength half is disclosed but is not
treated as persuasive by itself.

## Unchanged H-alpha transfer

The stellar-selected tensor is transferred to the 25 primary galaxies with complete H-alpha
kinematics without formula or parameter reselection. It is 70.53% better than the separately fit
high-dimensional flexible baseline, but **8.09% worse than zero alignment**. The frozen H-alpha
transfer gate therefore fails. This mixed result does not support a common matter-response law.

## Controls and compute

- All four synthetic niche injections are recovered in the correct niche in all five folds.
- The zero-alignment control selects only small nonzero fields and cannot improve on exact zero.
- The analytic local-response gate passes.
- CPU and RTX 5090 predictions agree within `4.77e-7` degrees.
- A continuum-field audit on the primary map subset finds a maximum 61.24-degree bend among the admitted library, above
  the frozen 45-degree ceiling. This independently fails the full-library physical gate; the
  threshold is not changed after response access.
- The RTX 5090 screened 2,797,178,216 response-blind pixel-field values and 4,280,875,200
  observed-plus-null residual evaluations. Candidate construction took 4.07 seconds and the full
  frozen evaluation took 6.04 seconds.
- Confirmation maps downloaded: **0**. Confirmation responses read: **0**. Post-response formulas
  generated: **0**. Paid calls: **0**.

## Interpretation

In plain language, the search repeatedly prefers a rule in which the orientation of a light
boundary, not just the amount of light, nudges the predicted stellar rotation axis. That stable
family choice is more interesting than five unrelated winners. It is still not a discovery: a
large formula search finds equally impressive improvements in randomized data, one major galaxy
slice regresses against the simplest physical null, and ionized gas does not reproduce the gain
against zero alignment.

The retained result is therefore the exact tensor family plus its counterexamples, not the claim
that gravity bends back into baryonic matter. A fresh replay must keep the continuum operator,
parameters or frozen fold consolidation, angular response, exclusions, and zero/structural/flexible
baselines unchanged. Only that independent result could promote it to a phenomenon lead.

## Next action

Advance in order to **Item 33, phase-transition gravity**, on fresh data. Freeze equal-capacity
critical-variable families, universal crossover scales, hysteresis/nonhysteresis distinctions,
local limits, strong ordinary change-point controls, and an independent motion or lensing response
before access.

Do not retune the 30 opened Item 32 stellar responses, use the 25 H-alpha transfers for new formula
selection, or download the eight confirmation maps. Preserve all four failure regions, 55,872
behavioral classes, the response-blind bend-ceiling failure, and the exact boundary-normal tensor
as an unchanged replication hypothesis. Continue Items 12/13, 20, 22, 25, 29, and now 32 only on
their separate frozen replication tracks.
