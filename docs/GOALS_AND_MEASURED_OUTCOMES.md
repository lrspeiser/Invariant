# Invariant goals and measured outcomes

This is the release-facing goal registry for the Invariant formula and proof discovery
system, current through **2026-08-14**. It distinguishes bounded engineering success
from mathematical, scientific, and operational completion. Counts below come from
checked receipts, tests, and the terminal workflow linked in the release section;
absence from a corpus is never treated as novelty, and a bounded proof is never
promoted into a broader scientific claim.

Overall status: **strong alpha; not yet scientifically or operationally complete**.

## A. Discovery platform

| # | Goal | Current measured outcome | Status / completion target |
|---:|---|---|---|
| 1 | Canonical candidate and provenance core | Eight artifact kinds, closed schemas, deterministic hashes, fail-closed stages/gates, and a hash-chained promotion ledger. | Bounded goal achieved. |
| 2 | Mathematical expression and proof layer | Exact expression IR, rational canonicalization, bounded counterexample search, rational-identity certificates, and induction certificates. | Bounded goal achieved. |
| 3 | Generator portfolio | Grammar, symbolic, evolutionary, Bayesian, e-graph, LLM, and cross-domain adapters exist: 7/7 families. | Implemented; scientific independence remains partial. |
| 4 | Common evaluation ladder | Typed PASS/BLOCK/REJECT/ERROR outcomes, hard gates, exact metrics, Pareto fronts, replay, and explanations. | Bounded goal achieved. |
| 5 | Domain-neutral Formula Discovery Job | Caller-supplied exact constraints now flow through rank classification, candidate construction, independent validation/counterexample, optional induction proof, Sigma Core provenance, deterministic reporting, orchestration, Pareto ranking, CLI exit codes, and immutable JSON/Markdown publication. | Product milestone achieved for bounded integer/rational linear-basis problems; broader nonlinear search remains open. |
| 6 | Knowledge graph and corpus boundary | Equation corpus: 18 artifacts, 31 nodes, 36 edges. Prior-art audit: 181 records; four alpha survivors absent from that snapshot. | Bounded implementation achieved; corpus breadth incomplete and absence is not novelty. |

## B. Discovery experiments

| # | Goal | Current measured outcome | Status / interpretation |
|---:|---|---|---|
| 7 | Complete blind benchmark curriculum | The superseding current receipt registers 200/200 slots with zero missing or invalid: 100 historical and 100 synthetic, split into 100 proof artifacts and 100 exact counterexamples. All 200 candidates were sealed before one atomic target-rule opening; all eight preregistered thresholds pass. The former 5/200 receipt remains bound as superseded historical evidence. | Bounded curriculum build-out achieved; performance outside the registered curriculum remains unclaimed. |
| 8 | Cross-generator alpha | Seven candidates: 4 PASS, 1 BLOCK, 1 REJECT, 1 ERROR; four entered exact Pareto ranking. | End-to-end pipeline exercised. |
| 9 | Prospective blind tournament | Three unseen worlds x seven families = 21 candidates; two worlds passed and one rejected. Both passes depended on Bayesian proposals. | Genuine bounded success; weak portfolio robustness. |
| 10 | Robustness and ablation | 8/8 CPU replay seeds exact; 168 evaluations and 336 gate comparisons stable. Removing Bayesian flipped both successful worlds; removing any other family changed none. | Reproducible but Bayesian-dependent. |
| 11 | Non-Bayesian recovery | Retrospective repair generated 48 candidates and recovered both frozen worlds. A prospective repaired tournament generated 72 candidates: 12 PASS, 60 REJECT, all three worlds recovered. | Breadth improved; worlds remain bounded and simple. |
| 12 | Blind semantic formula guessing | Three structured worlds x seven families: 0 PASS, 21 REJECT, 21 exact counterexamples. | Honest failure: hidden formulas were not learnable from the public input. |
| 13 | Constraint-conditioned semantic recovery | Quartic polynomial, partial fraction, and recurrence worlds: 21/21 candidates passed with exact certificates. Controls: 2 BLOCK, 1 REJECT. | Major bounded success. |
| 14 | Native independent discovery | The polynomial control remains 1 PASS, 2 REJECT. A second target-sealed tournament adds two non-polynomial worlds—linear-fractional rational and shifted exponential—across three native non-Bayesian families: 4 PASS, 2 REJECT, four exact certificates, two counterexamples, one denied pre-unseal probe exposing zero bytes, one atomic target read, and zero generic-solver, Formula Job, Bayesian, or post-unseal tuning calls. Every leave-one-family-out row retains a PASS for both classes. | Multi-world, non-polynomial isolation is achieved for these bounded classes; broader mathematics and open-ended discovery remain unclaimed. |

## C. Independent proof checking

| # | Goal | Current measured outcome | Status |
|---:|---|---|---|
| 15 | Real external proof kernel | Portable Lean 4.33 execution, path-free checked receipts, pinned toolchain metadata, and closed dependency manifests. | Achieved. |
| 16 | Known-formula rediscovery proof | The natural-number sum was blindly rediscovered and proved by induction in Lean. | PASS. |
| 17 | Bounded number-theory proof | For nonzero elements of `Fin 11`, the recovered exponent-10 identity was checked by Lean. | PASS; bounded finite theorem. |
| 18 | Generated-formula proof | Recovered recurrence `u(n)=2n^3+2n^2+n+7` was proved by Lean induction; a false base case was rejected. | PASS. |
| 19 | Proof-strategy breadth | Quartic and partial-fraction identities are independently replayed with exact integer coefficient arithmetic; the quartic convolution is checked by Lean and a `-30` to `-29` false control is rejected. Formula Job translation executes generated polynomial and recurrence sources in real Lean. Proof/Lean v2 adds three generated Lean 4.33 cases: a rational identity with an explicit nonzero premise, a two-prior-term recurrence, and a quantified non-identity proved by order contradiction. All 3/3 positive sources pass and all 3/3 single-token false controls are rejected. | Bounded multi-strategy acceptance achieved; general rational-domain translation and arbitrary higher-order closed-form proof synthesis remain open. |

## D. Gravity-theory science

| # | Goal | Current measured outcome | Status / missing work |
|---:|---|---|---|
| 20 | Known-answer formal controls | 118/118 portable formal controls pass. | Achieved as controls. |
| 21 | Local candidate health | All 12 quartic candidates pass local ADM rank/nullity, Dirac three-DOF counting, positive reduced quadratic Hamiltonian, and bounded local hyperbolicity checks. | Strong local result, not nonlinear/global closure. |
| 22 | Nonlinear second-variation coverage | Exact cross-direction replay sealed 5,082 additional off-diagonal entries per candidate. Coverage remains 5,324 of 257,499 ordered D2F entries per candidate (60,984 new candidate-bound records globally); 252,175 remain per candidate. A new audit shows that 22 formal direction records represent only 20 unique coordinate vectors. It constructs the entire 133-vector complement for all 12 candidates—1,596 exact tangent certificates in 96 chained checkpoints—so all 153 unique coordinate directions are now sealed. The arbitrary-background tensor formula projects all 99 principal second-jet directions, and the lower-coordinate gate projects the remaining 54 q/p directions, adding 648 candidate-bound certificates and preserving 24 alias reconciliations. The reachable-leaf completion validates all 31,680 A/B/C component-input roots: 7,920 P10 plus 23,760 Pother, partitioned into 396 nonzero and 31,284 exact certified-zero roots, and checks all 264 bounded ordered-D2 Merkle records. Successive scalar-gradient, remaining scalar-Hessian, metric-family (`s03`, `s01`, `s02`, `s12`, `s13`, `s23`, `s33`, `s11`, `s22`), and lower-`p0`/`p1`/`p2`/`p3` authority gates now seal 145 formal coordinate registrations. Each lower-family gate adds 10 formal columns and 316,800 exact candidate-bound leaf roots. A final-family alias audit shows that inherited `s11[10]` and `s22[10]` are duplicate aliases, so the measured unique coverage after `p3` is 143/153, not 145/153. The exact coherent missing family is all ten `q[0..9]` columns. The q-family checkpoint binds all 10 exact tangent packets and 200 scalar values. Unit 000 and the first two candidate batches now materialize 52,800 of 316,800 exact q-family leaf roots across 20 of 120 checkpoint units: unit 000 contributes 2,640 roots (311 nonzero, 2,329 zero), candidate 0 contributes 23,760 (2,799 nonzero, 20,961 zero), and candidate 1 contributes 26,400 (3,110 nonzero, 23,290 zero). | Coordinate-basis coverage, coordinate-to-covariant projection, 145 formal registrations, and 143/153 unique A/B/C leaf columns are complete. The q-family materializer has 264,000 roots and 100 checkpoint units remaining, so no q column is yet promoted. No projection or leaf certificate is misbooked as a new global D2 value: D2 remains 5,324/257,499 with 252,175 entries per candidate still open. |
| 23 | H6/H7 commutator closure | Finite hierarchy remains 12 BLOCK, 0 PASS. Representative slices cancel some terms, but the full high-atom identity and variable Bony remainder remain unclosed. | Major scientific blocker. |
| 24 | Nonlinear energy and lifespan | A candidate-complete terminal gate classifies all 12 candidates: zero global H7 proofs, zero closed bootstraps, zero positive lifespans, and 12 bounded BLOCK outcomes. Each candidate has an exact finite unmodified-Sobolev derivative-loss no-go, but those scoped no-gos do not exclude full-tensor modified energies, Nash-Moser, or analytic/Gevrey closure. The successor gate binds the broader System 8 result: all 15 independent-`G2` transport evaluations solve exactly, but the 55-state lift passes only `subset_0` and `subset_1` before `subset_2` fails at Taylor order three with 72 nonzero symmetrizer-remainder entries. This action-level obstruction upgrades 0/12 candidates to completion-grade. | All 12 candidates remain honestly BLOCKED, with zero rejection or promotion. The exact lift obstruction is not candidate-bound, provides no positive symmetrizer tube, and does not cover every admitted H7 closure strategy. The first missing primitive remains a candidate-bound full-tensor source/good-unknown `B7` bound or an all-closure-strategy completion-grade obstruction. Global H7, preserved bootstrap, and quantitative lifespan remain open. |
| 25 | Fourth-jet/full-direction closure | Thirteen local direction certificates exist. The prior BLOCK receipt remains historical evidence. The checkpointable materializer sealed 3/3 exact 55x55 P55 axis matrices with 144 sparse entries and zero residuals in 3,025 linearity and 3,025 sphere reductions. Candidate normalization, sphere reduction, and 15 order-zero P55 polarization packets first advanced the recurrence manifest to 34/304. A bounded formula specialization then constructed exact 11x11 `A_0`/`B_0` blocks and a symmetric 22x22 `h_plus_0`; exact projector replay constructed `K_0` with 131 nonzero entries and registered all 15 K55 order-zero packets. The exact order-zero TC2 formula `a10 P_0(n)(2e_33-8e_37)e_54^T` registers another 15 polarization packets. A checkpointable cold build then derived four independent order-one basis jets across all three axes, materialized 15 exact coordinate-free `P_1=M_0^-1(E_1-M_1P_0)` packets, and the sealed consumer advanced the manifest to 79/304. The apparent e1-only `K_0` limitation was resolved by a basis-free directional lift; its polynomial packet has 847 nonzero polynomial entries, 2,732 exact terms, maximum degree six, and zero residual across 3,025 sphere identities. Fifteen K55 and 15 TC2 order-one packets then advanced the manifest to 109/304. The higher-order materializer registers all 45 P55 packets for Taylor orders two through four: 15 polarization evaluations, 408,375 exact recurrence matrix entries reduced, and zero remainders. It advances the manifest atomically to 154/304. A source-derived companion lane materializes all 45 higher physical `H_star_plus` packets as exact zeros. The original `G1` transport ansatz fails at `subset_02`, but a strictly broader source-bound independent-`G2` recurrence solves all 15/15 transport systems exactly. The subsequent exact 55-state lift passes two evaluations, then fails at `subset_2`, Taylor order three: symmetry remainders are `[0,0,0]` while symmetrizer remainders are `[0,0,72]`. The manifest therefore remains 154/304 and the system still emits 0/117,180 coefficient rows. | The coordinate-free recurrence remains BLOCKED on 150 packets: 45 higher K55 packets, 45 higher TC2 packets, and 60 lower-Sylvester packets. Zero packets are inferred or partially registered. The transport primitive is now closed 15/15; the first exact missing primitive is the 55-state transverse cross lift of the independent-`G2` metric at `subset_2`, order three. Positivity, full D4/H7/PDE/lifespan remain open. |
| 26 | Universal matter PDE coupling | The minimally coupled scalar and irrotational `P(X)=kappa X^2` sector pass their four bounded gates; Maxwell has an arbitrary-background Hilbert-stress identity. The combined three-sector gate passes four matter-interface checks and has six-component polynomial `(|k|^2-omega^2)^5(|k|^2-3 omega^2)`. The census verifies 72/72 vacuum and matter-side prerequisites. All 12 candidates have unique total-action hashes and exact sourced metric Euler bindings covering 132 gravity rows. An arbitrary-point RNC derivation materializes the nonzero Maxwell-to-metric 4x10 mixed symbol, completing all 12 coupled 17-field principal matrices: 289 entries per candidate, 3,468 globally, zero unresolved. The registered q/v/w convention then closes all 12 exact first-order reductions at 85 states each (`17 q + 17 v + 51 w_i`), with zero 85x17 lift residual. The common-time audit passes 12/12 vacuum K55 and 12/12 matter prerequisites, and an exact nonzero-coupling witness is diagonalizable. A superseding full-block calculation constructs both degree-six Riesz projectors over the unit sphere (592 polynomial entries and 1,918 normal-form terms each), the full Maxwell cross pencil from 12 axis coefficients, and reduces all eight resonant products identically to zero modulo `n1^2+n2^2+n3^2-1`; the earlier companion-restricted packet serialization is explicitly non-authoritative for extension. The resonant and nonresonant Sylvester equations are closed: the latter contributes four exact solutions, 48 spectral block records, zero residual entries, and a uniform bound `2567/8 max_mu |B_mu|`. The corrected block symmetrizer closes the flat-sphere system on the nonzero domain `max|B_mu| <= 8/38505`, with exact Schur complement `H_g >= 1/7` and full lower bound greater than `1/28`; all finite `B` are pointwise positive at this reference, without an all-B uniform bound. | Total actions, sourced Euler equations, complete second-order principal matrices, 85-state reductions, and the bounded flat-sphere coupled symmetrizer are closed. Sourced gravity-constraint propagation remains a typed BLOCK: 12/12 matter-source divergences cancel and the common off-shell covariant identity is sealed. A resumable map registers all 780 readiness slots in 48 chained packets and emits 17 exact indexed tensor templates for `nabla Q`. At the flat constant reference it expands four gravity rows into 112 exact coefficients and binds the same row hash to all 12 candidates. A non-flat cylindrical `r=1` specialization materializes a complete 1,010-value packet (22 nonzero, 988 zero) and first closes 48 candidate-bound gauge rows. The exact normal/tangent operator then binds 48 sourced Euler projection skeletons to those candidate manifests and passes five independent Hamiltonian/momentum jet controls. The acceleration-elimination gate proves all 17 `partial_0 v` coefficients vanish, replaces all 51 registered `partial_0 w` atoms by spatial derivatives of `v`, and emits 12 Hamiltonian plus 36 momentum rows as 48 exact sparse polynomials with 30,884 exact sparse-polynomial terms. The full-domain lift closes the same 1,010 jet functions and all 96 physical gravity rows over `r>0`. The successor RHS chain registers 68 common kinematic rows, two matter dynamic rows, and four Lorenz-Maxwell dynamic rows: 74/85 RHS rows per candidate are closed and 11 gravity/scalar rows remain. The exact `div Q` materializer closes 4/4 rows with 191 nonzero operator terms, but no subsidiary propagation theorem follows. The gravity/scalar checkpoint seals 132 semantic A/W roots (121 A and 11 W) for a representative candidate. Its coordinate-arithmetic successor materializes all 121 A and 11 W entries across 11 exact rows with zero affine residual. A representative fixed-`r` audit then proves global invertibility false by an admissible rank-10 singular witness at `r=1`, `v_10=sqrt(3)/3`; on the preregistered tube `r=1`, `|v_10|<=1/2`, the determinant has exact absolute lower bound `3486784401/268435456` and all 11 accelerations solve with 11/11 zero residuals. The all-candidate successor materializes 12 candidate A/W packets with 132 rows, 1,452 A entries, and 132 W entries, and admits all 12 on the common tube `r=1`, `|v_10|<=1/4`. Its bounded tube solver closes 12/12 candidates with 132 exact acceleration formulas and replays 132/132 zero residuals. The linked common-tube RHS successor closes exact 85/85 rows for all 12 candidates on that same narrow tube: 1,020 row instances and equation-origin seals, including 132 new dynamic row instances with 132 exact zero-residual replays. Fixed-`r` positive and global-domain full RHS remain false. The open-`r` successor closes all 12 candidate packets with 132 dynamic RHS rows, 132 radial first jets, 132 Euler zero residuals, 132 radially differentiated zero residuals, and 132 exact `r=1` replays. Its tangential successor closes 264/264 direction-2/3 jets, for 396/396 first spatial jets total, with 264 differentiated zero residuals, 132 base replays, and zero unclassified `W` atoms. The exact off-shell chain then closes 48/48 candidate-by-lower-`nu` coordinate decompositions with 48 symbolic zeros; binds 96 physical constraints, 396 jets, 1,020 RHS equation origins, and four `div Q` rows; and closes 12/12 Euler-normalization bridges containing 72 `E_ij`, 12 gravity-scalar, 36 matter-force, and 48 `div Q` maps. Finally, all four `div Q` rows factor exactly through the modified-harmonic constraints as `Q=-(M2/2)hatP nabla C`, replaying 43, 55, 50, and 43 terms (191 total). The subsidiary initial-data successor closes 12/12 candidate maps. Its gravity normal-data matrix is `diag(-9/4,9/4,9/(4r^2),9/4)` with determinant `-6561/(256r^2)`, hence invertible on `r>0`; it also seals the Maxwell Lorenz/Gauss normal-data implication and registers five homogeneous subsidiary equations. Constraint propagation is still not claimed: the first missing primitive is a checked coercive energy/Cauchy-uniqueness estimate together with a bounded-tube boundary or causal-flux certificate for the coupled subsidiary system. No propagation, hyperbolicity, or global theorem follows from the algebraic initial-data map alone. Arbitrary formulation functions, global evolution, and physical-machine-scale validation remain open; this does not prove a global theorem or physical Jordan no-go. |

## E. Observational validation

| # | Goal | Current measured outcome | Status |
|---:|---|---|---|
| 27 | Solar one-shot trial | Two candidates BLOCKED; zero primary reads, held-out reads, or real evaluations. A current closure audits 10 bound metadata artifacts and finds zero authorized observational artifacts. Each candidate still lacks source-branch instantiation, split commitment, selected primary roots, and opening authorization: eight missing commitments total. The future opening-packet preflight is consume-once and target-free. The internal post-authorization boundary now includes an action-bound real-record likelihood executor contract, sealed against candidate/action, domain, split, record-root, parser, calibration, and authorization schemas. Four synthetic controls process 12 synthetic records with two PASS and two exact REJECT outcomes, while every real-data and observational claim remains false. | The internal execution contract is ready and fail-closed. The remaining blocker is exactly the eight external opening obligations; no data has been opened. |
| 28 | Galaxy validation | Generic evaluator: 70/70 BLOCKED. Reviewed registration has 11 entries with seven missing; only analytic/synthetic controls are admitted. | No real held-out pass. |
| 29 | Lensing validation | Schema controls pass; authorized real packets opened: 0; scientific result: one BLOCK. | Awaiting authorized data. |
| 30 | Cluster validation | Schema controls pass; authorized real packets opened: 0; scientific result: one BLOCK; rank writes: 0. | Awaiting authorized data. |
| 31 | One genuine observation trial | No candidate has completed a sealed, authorized, no-refit direct-observation evaluation. The latest authorization audit performed zero target reads and zero real-data evaluations; it proves that no in-scope opening authority currently exists. | Highest external scientific gap; a trial cannot honestly start until the sealed authority packet and action-bound evaluator both exist. |

## F. Operations and acceleration

| # | Goal | Current measured outcome | Status |
|---:|---|---|---|
| 32 | Persistent scheduler | Historical CPU campaigns processed 7,864,320 formulas. A current isolated rehearsal admitted three CPU tasks, deliberately expired one attempt-one lease, recovered it once with zero recovery failures, completed all three tasks with attempts `[2,1,1]`, and sealed checkpoint sequence 1. | Current scratch mechanics demonstrated without production-state access. |
| 33 | Current continuous operation | Formal batch 0005 advanced the cumulative cursor to prefix 17: 314 checked, 312 new, two reconciled, 314 REJECT, zero PASS/BLOCK/promotion, and 10,935 pending. The bounded batch completed two more leaves in about 80 seconds. | Consecutive bounded batches ran successfully; sustained multi-hour operation is not established. |
| 34 | GPU acceleration | 163 candidates; 87,509,958,656 measured formula evaluations; 5,341,184 GPU/CPU comparisons and 5,216 exact checks with zero violations. | Strong synthetic acceleration control. |
| 35 | LLM safety and spending | The core adapter discovers `ANTHROPIC_API_KEY` from the machine's `.invariant.env` without persisting the value. The current core receipt records 8 authenticated `claude-opus-4-6` calls, 29/29 retained ideas, 174 independent proof-plan branches, and 64 recombinations. A core-only provider wrapper injected one sealed creativity-first context into every call and bound its hash to all eight distinct API response IDs. Historical spending figures remain separate from this receipt. | Current-context runtime and non-pruning lineage are achieved. The receipt records `PASS_CONTEXT_BOUND_TO_AUTHENTICATED_CALLS`, 89,202 tokens, and no credential value or path. Comparative creativity value remains unproven until blinded matched-control reviews and component knockouts are complete. |
| 36 | Dashboard and recovery | Dashboard, immutable checkpoints, leases, and replay exist. The two-logical-host contract exercises generation-fenced sessions, host and lease heartbeats, dead-host/expired-lease recovery, stale-result rejection, chained-event tamper rejection, resume, and a measured combined SQLite/WAL/SHM ceiling. The real-duration campaign is now terminal: two cleanly stopped logical-host sessions, two succeeded packets, 21,600.000388 credited union seconds (`host-a` 17,227.661285; `host-b` 21,600.000388), 16 chained events at root `e082114a...`, and 53,248 SQLite-family bytes under the 536,870,912-byte ceiling. The sealed receipt explicitly records no physical two-machine or scientific-validity claim. | Durable mechanics, real six-hour logical-host duration, recovery, event replay, and byte ceiling are complete. Evidence for two physical machines and scientific validity remains open; production-scale scheduling and legacy production freshness remain open. |

## G. Reproducibility and release

| # | Goal | Current measured outcome | Status |
|---:|---|---|---|
| 37 | Focused alpha CI | The last terminal clean-clone alpha baseline exercised Formula Job/CLI/Lean translation, matter controls, P55 registration, native discovery, and proof breadth without a failing focused shard. The current Goal 25/26 release candidate adds a locally green 124-test integration bundle and 34-file Ruff surface. | The historical baseline passed; the new release candidate still requires its own clean-clone run. |
| 38 | Full workflow CI | [Run 31746356515](https://github.com/lrspeiser/Invariant/actions/runs/31746356515) remains the last fully terminal all-green receipt: 39/39 successful jobs at `bb76e8ec60325d72db69caf6d65d70ae4337f6d3`. The later `f620f1b` run exposed one typed-partition byte-authority failure and one 30-minute shard timeout; both fixes are now locally validated but not yet proven by a new terminal run. | A new 39/39 clean-clone receipt is mandatory before merge. |
| 39 | Human-readable evidence | Deterministic Markdown/Jupyter reports cover successful derivations, exact blockers, prospective tournaments, semantic 0/21 vs 21/21 contrast, identity breadth, a public CLI PASS/REJECT walkthrough, and the Goal 25/26 failure-to-construction progression with its remaining typed blockers. | Strong auditability. |
| 40 | Repository migration | Development, evidence, CI, and [draft PR #1](https://github.com/lrspeiser/Invariant/pull/1) live in `lrspeiser/Invariant`. The PR remains open, draft, mergeable, and currently unstable while this release candidate is validated. | Migration achieved; upload, terminal CI, review, draft conversion, and merge remain release work. |
| 41 | Release hygiene | The Goal 25/26 release boundary contains 75 exact paths and about 5.5 MB of ordinary-Git content, including three historical receipt byte-authority repairs. Git LFS tracks 294 historical objects with no object pending upload and no new file large enough to require LFS. Test basetemps, runtime state, and the 115-file D2 checkpoint tree are excluded; the remaining apparent tracked changes are materializer/line-ending noise. | Stage only the enumerated paths; never bulk-stage this materialized worktree. |

## H. First-principles derivation (added 2026-08-18)

Registers the 2026-08-15..18 sprint, which post-dates the 2026-08-14 boundary above.
Forward-looking claims, tests, and falsifiers for this section live in
[`FIRST_PRINCIPLES_DISCOVERY_GOALS.md`](FIRST_PRINCIPLES_DISCOVERY_GOALS.md).

| # | Goal | Current measured outcome | Status / missing work |
|---:|---|---|---|
| 42 | Derivation from declared constraints | Tensor-space constraint search derives the Einstein tensor by exhaustion: 10 enumerated contraction patterns at `d=4` order 2 (595 at order 4), collapsed by the Riemann symmetries and first Bianchi identity to a final family dimension of 2, `alpha G_mn + Lambda g_mn`. The Newtonian limit fixes `alpha = c^4/(8 pi G)`; `Lambda` comes out unforced and is reported rather than hidden. Jets are carried exactly over a prime field and replayed under a second prime; sampling error is one-sided, so uniqueness is closed from below by exhibiting `g_mn` and `G_mn` on held-out jets. The `d=4` order-2 search closes its sandwich (`sampled_dimension` 2 = `independently_exhibited_dimension` 2, `sandwich_tight: true`), so the Einstein result is sound. The Gauss-Bonnet `d > 4` claim does NOT: searches 2, 3 and 4 publish with `sandwich_tight: false` (exhibited 3 vs sampled 4; exhibited 4 vs sampled 5), and the `d>4` verdict is a difference of two open upper bounds. That generalization is not established. Receipt carries `novelty_claimed: false` and `framework_was_declared_not_discovered: true`. | The `d=4` Einstein derivation is sound. Two defects found 2026-08-18: (a) the `d>4` Gauss-Bonnet generalization rests on untight sandwiches and must not be claimed; (b) `declared_framework` is copied verbatim into the receipt at `:1788` and consumed nowhere, with 0 test references, so the receipt's stated boundary is unverified prose — a config declaring elasticity returns the GR basis unchanged. Domain transfer is blocked by a hardcoded Riemann basis generator, not unattempted. |
| 43 | Inverse variational engine | Derives Lagrangians from equations of motion, 1,910 source lines with 941 lines of tests. Landed 2026-08-18. | Achieved. The control battery exists and runs in CI: 78 Helmholtz conditions across 8 systems, 4 VARIATIONAL / 3 NOT_VARIATIONAL / 1 OUT_OF_DECLARED_CLASS, 13 conserved quantities, 65/65 tests. Damped oscillator and non-conservative planar curl are both correctly rejected. |
| 44 | LLM program synthesis under sandbox | FunSearch-style loop over executed programs, 2,949 source lines and 54 tests. The proposer is asked only for source, never for a result. `guard_prompt` refuses any prompt containing declared forbidden vocabulary, and the sealed answer table is unreachable from the prompt path. Sandbox containment is now kernel-enforced on both platforms: `RLIMIT_AS` on Linux, `JOB_OBJECT_LIMIT_PROCESS_MEMORY` on Windows. Measured LLM spend to date: 600 hundredths ($6.00). | Containment and blindness achieved. Discovery value is unproven: the LLM lane has never been ablated, so its contribution is unmeasured. |
| 45 | Platform-invariant receipt replay | An AST scan of all pinned source hashes on 2026-08-18 found 262 resolvable pins: **125 sealed against LF bytes** (portable) and **137 sealed against CRLF bytes** (Windows-locked). The portable set correlates with the ~123 `eol=lf` entries in `.gitattributes`; the intended workflow is pin-then-seal, and the 137 were sealed without the pin. Affected files read clean in `git status` because Git normalizes on read, while the receipts layer hashes raw disk bytes. | **Passing only via out-of-band normalization.** CI is green because `scripts/materialize_hash_bound_worktree.py`, invoked 7 times across the workflow, rewrites the worktree (2,006 files on Windows, 197 on Linux) until hashes match — so provenance depends on a step whose job is to edit files until they agree with their pins. `_canonical_lf_sha` already exists in 23 modules and is the correct primitive; the raw-byte `_file_sha` copies are the defective ones, making this a migration rather than an invention. The fix is to add the missing `eol=lf` pins and **re-run the campaigns** so receipts are regenerated, not edited — mass-patching the 137 SHAs to match current bytes is precisely the tamper `test_resealed_leaf_zero_D2_or_scope_tamper_fails_closed` exists to detect. Adding pins without resealing breaks the 137 on Windows too. Scope: ~40 modules. |
| 46 | Multi-model ensemble proposer with attribution | The proposer now draws from a declared ensemble of models with integer weights instead of one fixed `--model`, and every sealed program records which model returned it. Weights are integers so the call schedule is a largest-remainder allocation over integers: the realized allocation is checked by cross-multiplication (`|c_i*W - w_i*k| < W`) with no floating point, and a full cycle realizes the declared weights exactly. Per-model yield reports calls, spend, programs returned, programs first sealed, distinct behaviours contributed, behaviours no other model contributed, and best quality reached. Cost runs through the existing `SpendGovernor` with the cap unchanged; the only addition is that one call may cost several units of the base charge. Measured on the sealed mock campaign (`runs/math/model-ensemble/ensemble-v1.json`, `content_sha256` `6650cb2e1adea...`): 48 calls, 132 simulated hundredths, 192 programs, 141 distinct behaviours. The cheap model took 3/11 of the spend for 38/47 of the behaviours only it contributed; the model priced eight times higher took 8/11 of the spend for 5/47, so its `cost_verdict` is `did_not_earn_its_cost`. Against the equal-**dollar** single-model control -- the same 132 hundredths spent entirely on the cheap model, 132 calls instead of 48 -- the ensemble lost on both axes: 141 behaviours against 436 and best quality 0.982271003 against 0.993890818. | Mechanism achieved; the ensemble it was built to justify was refuted at this cost ratio. This is the first ablation the LLM lane can actually run (Tier 6 L2): entry 44 records that the lane's contribution was unmeasured, and it is now measurable per model. Two declared limits keep the verdict honest: the leave-one-model-out figures are a counterfactual over the sealed population rather than a re-run, so the survivors never get the removed model's budget back; and the behaviour clustering can split a behaviour but never merge two, so unique-behaviour counts are an upper bound. Both biases favour the expensive model, which is why `did_not_earn_its_cost` is the strong verdict here. The measurement is on the deterministic mutator, not on real models -- the numbers establish that the question is answerable, not what the answer is for haiku against opus. 49 tests, zero provider calls, zero real spend. |
| 47 | Symmetry- and dimension-forced derivation | A single declaration engine computes primitive invariant-coordinate bases for simple-pendulum, Kepler, diffusion, Reynolds, and drag controls. Fraction Gaussian elimination and SymPy exact nullspaces agree on rank in all five cases. Four controls have one coordinate; drag has two—drag coefficient and Reynolds number—so the core prompt now carries `F(pi_1, pi_2)` rather than assuming every search has one scalar. Six coordinate-offset mutations, five collapsed-basis mutations, and five deleted-nuisance-symmetry mutations reject. The sealed receipt stores five LLM-ready briefs with the required origin labels, and the core live path injected the complete basis set with typed grammar and proof-route context into all eight current provider requests. | D4 now covers bounded one- and two-coordinate declared diagonal-action controls, with authenticated prompt consumption proven by a shared context hash. It establishes exact admissible bases, not a specific physical law, empirical fit, or literature novelty; learned diagonal and state-pair actions are tracked separately in Goals 48–49. |
| 48 | Learn invariant coordinates from transformed data | A target-blind exact learner factors paired positive-rational transformation ratios into action rows and independently checks ranks/nullspaces with Fraction elimination and SymPy. It learns a two-coordinate drag subspace that survives unseen compound rescalings; a hidden deployment action rejects two train-only coordinates while retaining a one-coordinate repair; a rank-deficient action set preserves three coordinates and returns `UNDERDETERMINED`. Seven training candidates and six repaired/surviving coordinates are bound into three core Claude briefs, including failed and ambiguous branches. | Exact diagonal-action learning, core prompt binding, and authenticated consumption by all eight current Claude calls are achieved on three synthetic controls. This does not learn a scientific law or establish novelty/creativity. Remaining work includes noisy, missing, censored, and externally signed transformation datasets. |
| 49 | Learn matrix and nonlinear invariants from state pairs | A target-blind degree-two polynomial learner builds monomial difference matrices from exact rational before/after pairs and solves each nullspace independently with Fraction RREF and SymPy. It recovers `x^2+y^2` from orthogonal pairs, trace and determinant as the two algebraically independent coordinates of a three-dimensional 2x2 conjugation-invariant polynomial space, and `y-x^2` from nonlinear parabolic-shear pairs. All five linear-basis coordinates survive held-out pairs; a Jacobian-rank filter removes the redundant trace-squared coordinate from the creative coordinate set. The learner receives no action matrices, group parameters, named formulas, or target coefficients, and all three source-bound briefs are injected into the core Claude context. | Exact degree-two matrix/nonlinear state-pair learning is achieved on three known-answer controls. Typed higher-degree, Laurent, and formal-log extensions are tracked separately in Goal 52. Pair replay does not recover an action generator or prove global invariance, and it establishes no theorem, empirical law, novelty, or creativity advantage. |
| 50 | Retain invariant branches under measurement uncertainty | A target-blind bounded exponent learner gives exact, interval, missing, right-censored, and left-censored ratio observations executable semantics. It rejects a candidate only when an observable product interval excludes one, requires at least two evaluable training transformations, and keeps missing-active-variable outcomes unresolved. Four noisy transforms retain three compatible coordinates, two of which fail exact deployment; four partially observed transforms retain the single supported coordinate despite two missing replays; four censored transforms retain two set-valued candidates, one of which fails deployment. Six training branches and all three failed branches remain in the source-bound receipt and core Claude context. Finite bounded-corner enumeration independently agrees with monotone interval propagation wherever both apply. | Bounded marginal noise, missingness, one-sided censoring, exact deployment filtering, and non-pruning are achieved on three synthetic controls. Coupled dependence and exact finite unit hypotheses are tracked separately in Goal 51. Compatibility does not identify a unique formula, resolve uncertainty by assumption, or establish an empirical law/novelty result. |
| 51 | Preserve dependent-observation and unit-calibration branches | The same target-blind grammar now evaluates finite joint supports without replacing them by independent marginal boxes. On four coupled transformations the exact atom evaluator retains `a*b/c` and rejects three candidates that all marginal envelopes falsely admit. A separate control applies each declared ratio-calibration hypothesis globally across all training transformations: it retains `a*b/c` under `b_three_c_two` and `a^2/b` under `b_half`, never switches hypotheses per row, then held-out canonical-unit deployment keeps the first and records the second as a failed repair/recombination branch. The source-bound receipt carries five controls, nine training candidates, five deployment survivors, four failures, three rejected marginal false positives, and two unit branches into the core Claude prompt. | Exact finite dependence, unit-branch semantics, and authenticated consumption by all eight current Claude calls are achieved on synthetic controls. This does not learn a dependence model or unit calibration from observations, establish a unique formula, or prove novelty/creativity. Real external transformations, continuous propagated unit uncertainty, and detached signatures remain. |
| 52 | Learn higher-degree, rational, and transcendental state-pair invariants | The state-pair learner now accepts one of three bounded public feature grammars rather than assuming degree-two monomials. An eight-row rank-8 cubic control recovers `y-x^3` from nine features. A Laurent inversion control recovers the two-dimensional linear basis `x+1/x`, `x^2+1/x^2`, while the Jacobian filter retains only the first as algebraically independent. A formal-log control recovers `2 log(x)-log(y)` by turning positive-rational log differences into exact prime-valuation rows; independent trial division and SymPy `factorint` agree on training and held-out prime supports. Zero Laurent denominators and nonpositive logarithm states fail closed. Across six controls, nine linear-basis coordinates and seven algebraically independent creative coordinates survive deployment and enter the core Claude prompt. | Bounded cubic, univariate Laurent, and logarithmic representations plus authenticated consumption by all eight current Claude calls are achieved on known-answer synthetic controls. This does not cover multivariate rational denominators, exponential/trigonometric dictionaries, learn a transformation generator, prove global invariance, or establish novelty/creativity. External signed transformations remain. |
| 53 | Current-context Claude and current-main multi-host evidence | The default core-app credential discovery loaded the machine-local `~/.invariant.env`, injected `ANTHROPIC_API_KEY` only for the dynamic run, and completed eight distinct `claude-opus-4-6` Messages responses. Every call binds context `28c91b9f...ef3ea3b`, which contains five first-principles briefs, three learned-invariant briefs, six typed state-pair briefs, five uncertainty briefs, eight formula kinds, an expanded independent proof registry, and four origin labels. The run retained 25/25 ideas, compiled 6 executable hypotheses, scored 5 distinct candidates, and expanded 150 proof-plan plus 64 recombination branches. Separately downloaded artifacts from post-merge run `32683958812` bind four distinct Ubuntu/Windows runner IDs, two evaluator implementations per host, and one Lean-kernel artifact to main commit `58bbcb4...`; that CI run executes the piecewise-, transform-, tensor-, and variational-capable typed-proposal core. | Current core LLM participation and current-main multi-host reproduction are achieved with credential-free sealed evidence. Exact source/control profile matching is achieved; the LLM lane contributes 3 behaviors and 5 proof mechanisms beyond the deterministic portfolio in this specimen. Labels remain non-authoritative evidence, 19 non-executable hypotheses remain retained, and release remains blocked at zero level-5 successes with human review and live matched-control knockouts incomplete. The credential value and path are absent, all eight response IDs are distinct, and GitHub runner IDs prove distinct ephemeral VMs, not independent bare-metal machines. |
| 54 | Execute LLM tensor and variational proposals | The scalar proposal compiler now accepts a complete bounded tensor identity with shape, variance, components, symmetries, and an output component, or a first-order variational functional with formal symbols, a polynomial integrand, an Euler-Lagrange claim, and complete bindings to public aliases. Both routes execute through the existing SymPy-backed and SymPy-free exact evaluators; false identities return no prediction, malformed objects remain in the idea archive, and target-blind random controls match syntax depth, deterministic operation units, and verifier calls. A fresh default core-app run loaded `~/.invariant.env`, completed 8/8 `claude-opus-4-6` calls using 88,012 tokens, retained 26/26 ideas, admitted 5 executable hypotheses, and scored 3 distinct candidates. | Compiler and live prompt availability are achieved. The stochastic live run proposed 2 finite sums, 3 generating functions, 2 invariant relations, 3 recurrences, 1 modular relation, 4 scalar expressions, and 11 other typed relations—but no tensor or variational object. Therefore provider-path execution of these two new contracts is not claimed from this run; deterministic positive/negative execution and matched controls are proven, while an organically generated live specimen remains pending. Behavioral novelty is 1 and proof-mechanism novelty is 3 in this run; neither is a literature-novelty claim. |
| 55 | Execute bounded LLM transform relations | The proposal compiler now accepts a `linear_shift_stencil` with a public index, bounded exact rational coefficients and integer offsets, a source expression, and a claimed transform. It emits the claimed value only when a SymPy-backed rational replay and an independent Python-AST/`Fraction` replay both obtain the same exact stencil result. False claims emit no prediction; malformed objects remain active; matched random controls preserve syntax depth, deterministic operation units, and verifier calls. A fresh default core run loaded the machine-local credential without persisting its value, completed 8/8 `claude-opus-4-6` calls using 89,475 tokens, retained 29/29 ideas, admitted 6 executable hypotheses, scored 4 distinct candidates, and measured 2 behavior plus 4 proof-mechanism additions beyond the deterministic portfolio. | Claude organically proposed two transform branches. A self-labeled `known_rewrite` gives the forward difference of the sum-of-squares formula as `(n+1)^2`; safe single-variable normalization maps `n` to public alias `x0`, and both evaluators reproduce `1,4,9,16,...`. A second function-call/equality proposal is retained but not executed. This is live creative participation plus exact bounded replay, not a new formula or novelty verdict; tensor and variational provider specimens remain pending and release remains blocked. |
| 56 | Evaluate transforms on externally maintained structured material | Rotation `2026-08-23-002` adds four finite-difference-transform tasks from SymPy's current `test_finite_diff.py` to the existing four tensor and four variational tasks. The source is parsed as AST without import or execution; public generation tasks hide repository, URI, test name, and expected expression behind target commitments. A live build binds three complete response hashes, and exact refetch reproduces the 12-task pack. One opened transform target independently records the exact weights `[-3/2, 2, -1/2]` for sample points `[5, 6, 7]` at `x0=5`. | External transform coverage and a second structured rotation are achieved. All three files are maintained by one external principal (SymPy), upstream assertions are targets rather than proofs, and HTTPS response integrity is not a detached signature. The pack therefore remains usable for blind creativity evaluation but ineligible for level 5 until a distinct principal signs it. |
| 57 | Execute and retain ordered piecewise laws | The immutable math IR now admits one to eight ordered exact predicates with explicit expressions and a mandatory default branch. The SymPy-backed and Python-AST/`Fraction` evaluators agree on the positive absolute-value control and reject its claim mutation; branch-count, exact-rational boundary, singular-predicate, malformed-comparator, serialization, and matched-control tests pass. A fresh default-path core run completed 8/8 authenticated calls with 86,772 tokens and bound the eight-kind context into all calls. Claude produced nine piecewise ideas spanning `known_rewrite`, `cross_domain_synthesis`, and `uncertain`; every branch remains in lineage. | First-class and LLM proposal availability are achieved, including exact ordered boundary semantics and current authenticated prompt consumption. None of the nine live piecewise proposals executed because they use parity, floor division, heuristic rounding, or floats outside the admitted exact slice. This is retained failure evidence and selects exact bounded modulo/floor support as the next repair; it does not establish creativity superiority, formula correctness, or novelty. |

## Current priority order

0. **Find a creative new law that fits the data.** This outranks every other item here.
   The engine has ~132 gate/audit/control modules against ~26 generative ones and zero
   novel results; the binding constraint is proposal quality, not verification rigour.
   The credentialed LLM proposer is now live in the core app; the current receipt retains 29/29
   schema-admitted ideas, compiles 6 hypotheses into executable form, scores 3 distinct candidates,
   and preserves their authenticated eight-call evidence across deterministic component rebinds.
   The exact prompt lane now carries a tested two-coordinate search signature and target-blind
   diagonal-action learner plus typed cubic/Laurent/formal-log state-pair coordinates and
   coupled/unit uncertainty branches. Next, score the preregistered old-vs-new creativity ablation,
   extend learned invariants to dependence-aware real observations, learned/continuous unit
   calibration, and multivariate rational/exponential/trigonometric transformations,
   preserve every blocked idea as a repair/recombination branch, and upgrade the FunSearch
   loop toward AlphaEvolve (diff-based edits over whole modules, a MAP-Elites archive, an
   evaluation cascade). Guard work is scheduled only when it unblocks a search.

1. Preserve the 39/39 release baseline on every integrated change; never trade a
   scientific increment for an unverified release head.
2. Register the remaining 150 coordinate-free recurrence packets (45 higher K55,
   45 higher TC2, and 60 lower-Sylvester), emit the 117,180 exact
   rows, and resume the full-sphere D4/H7 chain.
3. Close the global H7 energy, tube-bootstrap, and quantitative-lifespan chain, or
   produce a decisive exact obstruction that terminates the candidate family honestly.
4. Materialize the 132 sealed gravity/scalar A/W roots into coordinate arithmetic,
   close the remaining 11/85 RHS rows, and use the exact 4/4 `div Q` rows to pursue
   sourced constraint propagation before widening the cylindrical domain.
5. Sealed no-refit Solar trial: OFF the critical path. It is publication infrastructure,
   not search infrastructure, and has produced zero target reads since it was registered.
   Keep a cheap held-out split during exploration instead, and revisit the formal protocol
   only when a candidate is good enough to be worth spending it on.
6. Extend exact D2F coverage beyond 5,324/257,499 entries per candidate, with every new
   block carrying replayable leaf/root provenance.
7. Let the currently running two-logical-host campaign reach its honest >=6-hour
   duration gate, then archive its terminal queue, recovery, byte-ceiling, and receipt evidence.
8. Extend the now-complete 200/200 bounded curriculum with genuinely external,
   independently sourced targets while preserving the frozen isolation and proof rules.
9. Expand the 181-record prior-art snapshot using independently sourced corpora and
   externally checked equivalence controls; corpus absence must remain non-novelty.
10. Resolve review feedback, reproduce the release from a clean checkout, and convert
    draft PR #1 into an intentionally reviewed merge-ready release.
11. Restore platform-invariant replay (Goal 45): pin the missing paths `eol=lf` and
    regenerate the 137 CRLF-sealed receipts by re-running their campaigns. Until this
    closes, every receipt's provenance is weaker than claimed, because verification
    depends on which operating system runs it.
12. Ablate the LLM lane and its lineage/non-pruning components (Goal 44). Use the result to improve
    prompts, roles, and resource allocation; do not delete an idea lineage or capability because of
    one underpowered or static-pack comparison.
13. Build the declaration layer from `LANGUAGE_PROPOSAL.md`, so a new capability is a
    declaration rather than a hand-written module. 122 of 125 capability gaps are open,
    and each currently costs one module; this is the throughput bottleneck.

The system is credible as an exact, auditable formula-recovery and proof pipeline with
documented success and failure modes. It is not yet a comprehensive theorem-discovery
system, a nonlinear gravity theorem, a universal matter result, or a validated empirical
discovery product.
