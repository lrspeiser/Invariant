# Gravity hidden-variable and theory search execution ledger

This is the mutable status companion to
`docs/GRAVITY_HIDDEN_VARIABLE_AND_THEORY_SEARCH_GOALS.md`. The goals file defines the stable
72-item search and testing contract; this ledger records what has actually been attempted.
Its embedded execution table is a frozen legacy snapshot because the Item 1 receipt binds
that entire file byte for byte. Update this companion ledger, not the goals file, when new
attempts finish.

| Item | Current status | Evidence | Required next move |
|---|---|---|---|
| **1. Effective dimension** | **INCONCLUSIVE** — the specific `beta=1/D_support` rule is rejected by the real cross-scale diagnostic; continuous baryonic mass-profile dimension does not predict the response; a binary disk/cluster label reproduces population means but is not a cause. | `GRAVITY_ITEM1_EFFECTIVE_DIMENSION_RESULT.md`; `runs/gravity/roadmap/item-01-effective-dimension-v1.json` | Retain as a counterexample and dependency for later cross-scale laws. |
| **2. Shape and anisotropy** | **SCOPED REJECT; ITEM COMPLETE** — five target-blind real-data attempts cover global projected shape, 2-D multipoles, comparable stellar tracers, radial/nonlocal shape, and intermediate group graph filamentarity. No family survives within-population, overlap, strongest-baseline, quality, permutation, and response-robustness requirements. Attempt 5's strong raw group prediction (`R^2=0.646`) is already matched or exceeded by nonshape nuisance variables (`R^2=0.655`). This rejects the tested projected-summary families as a sufficient universal cause, not every possible anisotropic gravity theory. Every Item 2 confirmation set remains sealed. | `GRAVITY_ITEM2_SYNTHESIS.md`; `runs/gravity/roadmap/item-02-synthesis-v1.json`; all five attempt documents and receipts | Advance to Item 3. Preserve Item 2's failure families for equivalence rejection; do not retune them on the same responses or open their confirmation sets. |
| **3. Surface versus volume density** | **SCOPED REJECT; ITEM COMPLETE** — attempt 1's coarse cross-scale and fresh-group transitions fail but have representation defects; attempt 2 repairs those defects with smooth direct galaxy surface-density and X-COP gas-density/Planck-pressure profiles. Ten galaxies and all eight exploration clusters pass quality, yet every fold selects the forbidden population label. The qualifying density selector has within-galaxy `R^2=-3.178`, within-cluster `R^2=-0.656`, loses to the acceleration baseline in both, has permutation `p=0.696`, and is not rescued by measured cluster stellar mass. This rejects the tested local amplitude/contrast/transition/crossover families, not nonlocal or field-equation density theories. All Item 3 confirmation sets remain sealed. | `GRAVITY_ITEM3_SYNTHESIS.md`; `runs/gravity/roadmap/item-03-synthesis-v1.json`; both attempt documents and receipts | Advance to Item 4. Preserve the two exact density rewrite regions in equivalence detection; route materially nonlocal density ideas to Items 9–11 or 45 rather than retuning Item 3 on opened responses. |
| **4–72** | **NOT YET EXECUTED under this roadmap ledger** | Existing repository work may supply controls or predecessor evidence but does not count as a roadmap PASS without an item-specific receipt. | Begin Item 4 baryonic compactness, then follow the numbered dependencies and real-test rule in the stable roadmap. |

Every row remains subordinate to its immutable JSON receipt. If prose and a receipt differ,
the receipt controls until the discrepancy is reviewed and corrected.
