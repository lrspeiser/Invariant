# TWELL-400 source-shaped rebind/replay v2 — append-only repair brief

## Frozen v1 disposition

`open-gravity-twell-400-source-shaped-rebind-replay-v1` is numerically sound but
mechanically blocked.  Its frozen module and test fail `ruff format --check`,
while the receipt/hand-off described Ruff as passing without disclosing that
format gate.

Independent evidence:

- audit program:
  `work/audits/audit_twell_400_source_rebind_v1.py`
- blocked audit artifact:
  `work/audits/open-gravity-twell-400-source-shaped-rebind-replay-v1-independent-audit-blocked-babbeb72.json`
- blocked-audit raw SHA-256:
  `41f5ae2d01aea055ac10373b7789fe7cba0da543f9d73e253dde0273dac502e1`
- blocked-audit content SHA-256:
  `786ac589b8e2221d7679bb58d4c2b1e2338dddda74893c82d1d6bd8fb912da24`

The independent auditor used no subject-module execution path. It imported only
the separately frozen static radial adapter and independently reconstructed all
2,592 admitted X-COP object/formula/cell executions. Exact results:

- 5,184/5,184 factor and effective-acceleration arrays matched byte-for-byte;
- 0 prediction, metric, status or hash mismatches;
- 2,554 completed + 38 numerical-invalid unique executions;
- invalid formulas: A11-D03 16, A11-D06 16, A11-D04 5, A11-D07 1;
- 62,208 replay rows = 61,296 completed + 912 invalid;
- 122 finite-source prediction-tie groups, none promoted to formula identity;
- 0 response values and 0 scientific scores.

## Required v2 repair

1. Preserve v1 config/module/test/receipt and all ten v1 result artifacts
   byte-exact; bind their raw/content hashes.
2. Create a new append-only v2 config/module/test/receipt and output directory.
3. Apply only formatting/mechanical-path changes needed for:
   `ruff check` and `ruff format --check` to pass.
4. Bind the blocked audit above and state that it independently establishes the
   v1 numerical result while rejecting the v1 mechanical release claim.
5. Validate the existing v1 artifacts rather than regenerating or altering the
   scientific arrays. If a v2 mirror is emitted, require byte identity for every
   science artifact and make the lineage explicit.
6. Retain exact counts, invalids, access accounting, claim ceiling and all source
   incompatibilities. No response decode and no scientific score.
7. Run focused predecessor+v2 tests, `ruff check`, `ruff format --check`, and a
   deterministic no-clobber check from an alternate working directory.
8. Hand off exact hashes to a distinct auditor. Do not self-claim independent
   PASS.

Claim ceiling remains `SYNTHETIC_DIRECTIONAL_SIGNAL`. This repair does not make
the other 274 cards source-executable; it only makes their explicit blocked or
incompatible dispositions mechanically admissible.
