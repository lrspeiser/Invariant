# Chandra level-2 event lists: 246 MB across 27 observations, and they are
# re-fetchable from the public archive by the obsid + URL + sha256 recorded in
# raw_manifest.json. Same pattern as the >50 MB raw catalogues elsewhere in the
# programme: the hash pin is tracked, the bytes are not.
raw/
