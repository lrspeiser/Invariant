# Post-hoc cored clock repair, frozen before this experiment

This is a developmental response to the already viewed run001 finding of central overprediction (parent reports positive inner bias in 84/96 galaxies with inner coverage). It is not fresh confirmation, and no improvement establishes time-energy exchange. The source/data admission remains SPARC radial empirical comparison only, with the original 139 exposed identities and exact 102-object eligible cohort.

Freeze two added-acceleration families, both `gextra=beta GM r/[(r+d)^2(r+d+B)]`, with d=published Rd and B=GM/Psi0. Family `clock_core_original_scale` uses Psi0=lambda a0 d. Family `clock_core_mass_scale` uses Psi0=lambda sqrt(GM a0). The same published-force baseline and photometric/HI M proxy as run001 are used. Both give gextra proportional to r at the center and finite total effective source at infinity. An intermediate 1/r regime exists only where d << r << B; it is not guaranteed for every object.

Each family freezes 72 candidates: mass factors [0.8,1,1.2], strengths [0,0.3,1,3,10,30], lambda factors [0.1,1,10,100]. No expansion or retuning after scores. Five whole-galaxy folds and the same three original seeds; equal-galaxy mean squared log-speed loss; parameters chosen from training galaxies only. Save all training losses, choices and held predictions. Compare to original clock and fixed/adjusted MOND using their original held predictions. Report inner r/Rd<1, middle1..3 and outer>=3 signed log-speed bias and RMSE, with equal galaxy weight within region.

Before loading response rows: independent manufactured checks for potential gradient via improper quadrature, positive/saturating enclosed effective source, central/outer limits, zero-strength Newton recovery, dimensional scaling, frozen count and train-only selection. With H=r^2 gextra/(beta GM)=r^3/[(r+d)^2(r+d+B)], H'/H=3/r-2/(r+d)-1/(r+d+B)>0 for d,B>0; H tends to1 at infinity. Potential Phi=-integral_r^infinity gextra(s)ds. Gradient check relative tolerance1e-6; numerical comparisons1e-10 unless explicit asymptotic tolerance1e-5. Keep failures, no response-dependent fixes/exclusions.

This repair is an added static phenomenological potential, not a 3D clock map, measured delay, or conservation law. The mass proxy remains simplified and published distance/inclination fixed. The same archive hashes and cohort are required. No reserved member bodies are opened. Any gain is hypothesis development on an exposed sample.
