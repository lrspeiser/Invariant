# Prospective numerical admission design, execution034

No observed source spectra are read by this design. Catalogue1512 fixed cases: baseline h.1/h.4 and common30/common30stars_h.4/missing_zero/missing_annular, each Newton/Newton+log, pressure5/10/15, spins±, three spectral histories, seven previously declared material factors. Preserve all prior signed-centroid failures.

The prospective centroid criterion uses positive pre-continuum stored-window emission, because033 demonstrated signed post-continuum cancellation. This is a new diagnostic contract, not a relabeling of old failures: signed-profile L1<1% remains; positive stored-window centroid refinement<.5km/s. Neither diagnostic determines which theory fits observations. Keep finite unknown-emission envelopes, no invented rotation, no extrapolation. Record capture/cancellation separately if needed, and all numerical failures.

Four source quadratures and five comparison pairs retain all15apertures. Evaluate truth[0,10,1] and full eight corners of systemic±30, sigma3/20, multiplier.5/2. Because the renderer and both diagnostics are homogeneous in positive multiplier, compute each unique systemic/sigma once and reproduce corner amplitudes by exact scaling. No source or velocity fitting. Later parameterpoint routine evaluates all four quadratures at the supplied actual fit vector; passing corners is not assumed to prove the entire box.

Sources/contracts/pressure/geometry/operator limitations are inherited from032/033. Material factors apply to stellar/HI/CO forces as declared; HI factor also multiplies emissivity once and cancels between matched pressure derivative/raw denominator. FixedMATERIALS are baseline,(2/3,1,1),(4/3,1,1),(1,.8,1),(1,1.2,1),(1,1,.5),(1,1,2). Finite-support and negative-v² masks recomputed per case. Bound signed output at multiplier2. Prechecks compare GPU signed spectra to inherited callback and positive spectra to independent CPU CDF/instrument reference before allcase evaluation. Observed scoring remains parent's separate authorized workflow.
