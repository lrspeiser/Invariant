# Full-padding branch frozen after geometry, before background access

Nine western and seven eastern centers meet the complete163-square guard. Execute the full empirical30arcsec detector on these centers; this uses real surrounding noise, with no zero padding inside the81-square detector crop. Fit detector per-channel mean and population variance over all equally sized western81-square detector fields only. Save them before opening eastern padded values. No western tuning or selection. Full-detector amplitude units are5/10 times median western detector sigma. Separately retain the fixed-model native-core amplitudes5/10 times median predicted native sigma; these are different specified experimental strength scales, not identical absolute brightness across the two detectors.

The full detector has noiseless and seven empirical trials per case; do not manufacture an81-square Gaussian field from the24-square fitted covariance. Gaussian32-draw predictive calibration and all18 empirical-core GLS/mask trials apply only to the native-core model. Both branches preserve matched morphologies and backgrounds. Real padded patches are larger and may overlap; their count is not independent sample size.

Render each source on121-square synthetic grid, convolve there and take an81-square centered output for the full detector, and the declared24 crop for the core model. Include121-square positive restored flux in the reference denominator and quantify each crop's captured fraction. Shift synthetic centers by -4,0,+4 pixels, which implements comparable positions without shifting or padding actual noise. A161-square rendering control bounds numerical truncation separately from the deliberate crop loss. Template offsets still use the predeclared elliptical brightness and synthetic velocity pattern. All72 native output channels retained. No thresholds adjusted after trials.
