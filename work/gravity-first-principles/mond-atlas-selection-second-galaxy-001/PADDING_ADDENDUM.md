# Before further array access: attempt full detector geometry

Parent requested an explicit geometry-only attempt at full empirical 30-arcsec detection before accepting the restricted native-core diagnostic. Original preflight did not evaluate padding outside the packet. Preserve it and this addendum; no cube values or injection outcomes have been opened in this package.

Assess all18 fixed eastern core centers, at the integer center coordinates retained in the transfer geometry. Synthetic/source field is81x81, detector extra-Gaussian radius computed from NGC3198's own restoring beam to reach30arcsec FWHM with5-sigma truncation. Required real patch radius=40+extra-kernel-radius. Every required pixel must lie inside image42-pixel edge guard, east>90arcsec and outside the120arcsec expanded positive/nonfinite complete MOM0 footprint. Annulus550–680arcsec applies to the retained center; the larger patch may extend outside that annulus, explicitly broadening its sampled sky geometry. This guard is checked before any new background pixels; preserve every rejection reason. Larger patches may overlap, so no independent-trial claim.

If at least3 centers qualify, run full detector injection trials there using native plus real smoothed backgrounds, fixed native model threshold inappropriate to smoothed field: estimate detector per-channel variance/mean exclusively from equivalently eligible western patches, requiring at least3 western centers. No eastern fit. The separate24core GLS test keeps its fixed model. If either side lacks3 eligible patches, record insufficient geometry and proceed only with the original explicitly restricted native diagnostic. Never shrink exclusion, fabricate background padding, or infer larger-field covariance from the24core model.
