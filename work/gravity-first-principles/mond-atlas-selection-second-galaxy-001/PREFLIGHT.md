# Frozen NGC3198 local injection transfer

SOURCE_BLOCKED for observational galaxy likelihood; permitted conditional background/injection mechanics only. Read docs/OPEN_GRAVITY_BUILDER_SOLVER_ADMISSION_POLICY_V1.md. Sources: THINGS native NGC3198 header and already-exposed screened 20-west/18-east core packet from mond-atlas-noise-transfer-001, Walter et al. 2008 https://arxiv.org/abs/0810.2125, archive https://things.www3.mpia.de/Data.html. Bind exact transfer model and native history. No observed source velocities, new downloads or reserved galaxies.

The 24x24 (36-arcsec) cores cannot support the previous 81-pixel patch plus extra 30-arcsec detector convolution. Do not zero-pad observed noise or claim that old detector transfers. Instead freeze a native-resolution mask: greater than twice the fixed model's per-pixel/channel marginal sigma for three consecutive channels, no spatial convolution of data. Gaussian simulations use the same complete fixed DCT noise model as the transfer, not a newly selected beam-noise model. Native restored signal can be rendered on a padded synthetic field and cropped exactly. All flux outside the core must remain in the full-signal denominator and be reported explicitly.

Use the existing rotation/warp/streaming synthetic velocity patterns: elliptical Gaussian major FWHM30arcsec, axis ratio.5; local Gaussian line FWHM2 stored channels; rotation4*tanh(R/15)*X/R, twist30deg*R/(R+15), streaming extra2*tanh(R/15)*Y/R. Fixed line centers stored indices10,36,61; synthetic angular centers at native crop center and +/-4 native pixels along x; identical placements/backgrounds across morphologies. Render 81x81 about pixel39.5 and crop [28:52] in y and x shifted by -4,0,+4. Full81-field flux (checked against larger121 field) is reference, with core capture reported separately.

Use NGC3198's actual 108 parent channels, stored indices19..90, UVLIN order1 and documented fit indices. Reconstruct the image-domain continuum surrogate from this galaxy's own history. Restoring Gaussian comes from its own AIPS CLEAN beam and signed SIN pixel scales. Bracket independent boxcar, full Hanning and decimated Hanning pre-grids; these are conditional spectral responses, not an identification of the archived correlator or visibility operator. Apply spectral filtering, restoring convolution and continuum surrogate to synthetic signals only; do not reprocess already observed background noise. Dirty beam, CLEAN residual scaling, per-visibility flags and primary beam remain unresolved.

Normalize morphologies jointly to the full rotation template's positive native peak for each spectral branch/center. Multiply by 5 or10 times median fixed marginal noise sigma. All162 cases retained (3 branches*3 centers*3 placements*3 morphologies*2 amplitudes). Evaluate noiseless, all18 eastern cores, and32 independent conditional Gaussian draws, seed9063001. No western refit or model selection. Save full-positive reference flux, captured positive fraction, selected true/full and true/core fractions, signed post-continuum/core ratio, selected noisy flux, paired selected flux minus same-background baseline, peak selection and selected voxel fraction.

Also use the full fixed covariance, without threshold selection, for the known-template generalized-least-squares amplitude: a_hat=t^T C^-1 y/(t^T C^-1 t), variance=1/(t^T C^-1 t). This is an oracle known-template test, not blind morphology discovery. Report standardized errors, empirical spread and conditional Gaussian mean-square standardized error per case. Matched template/amplitude noise errors are deliberately paired across injection amplitudes; do not count as independent evidence. Finite training-covariance and fitted-mean uncertainty are not integrated; eastern cores may be dependent.

Before empirical packet access: independent mask loop, scalar/dense GLS identity, Gaussian known covariance calibration, zero-motion identity, flux invariance, exact continuum reproduction of constant/linear parent spectra, beam/unit-sum and padding checks. Freeze all outputs/model hashes before trials. Descriptive recovery mean true/core>=.9 and paired/full within.1 of1 are separate flags, not significance tests or admission; full recovery may be impossible because cropped tails are explicitly counted. No retuning. Independent arithmetic replay required. <=5minutes one CPU thread, public<20MB; synthetic templates/arrays may remain private<100MB. No observed cube likelihood admitted.
