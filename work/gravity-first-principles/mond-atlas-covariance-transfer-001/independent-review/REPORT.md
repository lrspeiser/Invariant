# Independent covariance transfer review

All1,392 fit records and4,176 optimizer-start records pass replay. Every admitted evaluation has a preceding numerical freeze record with the correct prediction hash, unchanged fit parameters and the original disjoint10/5 aperture partition. Independent dense covariance solves reproduce held discrepancies and their baseline shifts with maximum absolute error6.66e-16. Parameter shifts and all summary medians/extrema also match. All bound inputs match their recorded hashes.

Code inspection confirms only training spectra enter fitting, the lowest successful training loss selects parameters, and no held discrepancy selects a covariance variant. All29 western leave-one-core-out variants remain reported. The journal is flushed before each held evaluation; record order is verified here, not a tamper-proof external timestamp. Three optimizer-start records are retained per fit, including their statuses; this review does not rerun optimization or independently recompute the Jacobian.

The recovered nuisance parameters are stable under this particular noise-sample perturbation: reported extrema are approximately .020km/s systemic shift, .0208km/s line-width shift and .002338 absolute amplitude shift (.234percentage points around unit amplitude). The largest absolute change in mean held discrepancy is .02471. These are sensitivity extrema over dependent variants/trials, not confidence limits or evidence that all source-region noise is modeled correctly.

The original mock signals and previously exposed backgrounds are reused. This tests noise-model transfer while retaining the valid-emitter conditional source and unknown-emission caveat; it does not compare gravity laws or score observed source-region spectra. Spatial covariance between apertures remains outside the working likelihood. The independent replay reuses the previously benchmarked GPU callback for three source truth signals and computes every held quadratic separately.
