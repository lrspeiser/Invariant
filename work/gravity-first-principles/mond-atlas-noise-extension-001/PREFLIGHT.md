# Western-selected channel covariance at each aperture

2026-09-06. BACKGROUND VALIDATION ONLY; source/emission likelihood remains SOURCE_BLOCKED. Public source NGC2976 natural-weighted THINGS cube, https://things.www3.mpia.de/Data_files/NGC_2976_NA_CUBE_THINGS.FITS; primary paper https://arxiv.org/abs/0810.2125. Reuse only hash-bound saved 29 western and 27 eastern external-background cores, 24x24 native pixels, 42 channels. Both sides historically exposed. No new raw files or cube/source/emission responses accessed. Existing mask is inherited, not newly certified clean noise.

Freeze before reading arrays: sides1,2,4,8,12,24. Keep channel-constant mean estimated on fitting western cores only. Geometry-defined three western folds from original block-geometry.csv; selection uses equal-core marginal Gaussian log density, averaged over western held cores. For each scale compare full channel second moment shrinkage [.1,.3,.6,1], Bartlett lag1,3,7 with shrinkage [.1,.3], and sample-shape mixture weights[0,.25,.5,.75,1] toward single-pixel correlation with per-aperture channel variances. The target single-pixel covariance uses shrinkage .1. The mixture sample itself uses shrinkage .1 to ensure SPD. Candidate declaration order breaks ties. No eastern selection or retuning.

Fit all candidates on all western cores after inner selection; write every fitted matrix, western ranking and choice before opening eastern validation member. Then report all candidates and every eastern core, selected q/N, trace ratio and marginal log density versus original fixed .1 full covariance. Fixed descriptive q/N [.8,1.2]; no independence-based significance. No outer likelihood optimization or mean subtraction. Mean uncertainty is not modeled.

Before actual array reads: independent explicit tile averages, independent Gaussian inverse/logdet, analytic diagonal scaling and PSD mixtures, rank-deficient small sample tests, planted held likelihood selecting diagonal over spurious correlations, and malformed data rejection. Algebra tolerance1e-10. Geometry audit validates counts, ordering and box nonoverlap; source hashes are checked. This tests separate marginal aperture covariances, NOT a joint spatial-channel cube process. Correlation between tiles/cores/scales remains; no claim of mask or source-likelihood admission.
