# Sparse-DC variance regularization and mean-estimation uncertainty

BACKGROUND DEVELOPMENT ONLY; source/emission likelihood remains SOURCE_BLOCKED. Same hash-bound29western/27eastern NGC2976 cores, three inherited geometry folds, previous regional exposure explicit. No new raw download or galaxy/source/velocity/lensing target. Five-minute runtime cap and20MB new output cap. Preserve all mode-power output and failures. Primary observation https://arxiv.org/abs/0810.2125.

Motivation: DC supplies only one spectrum per core, hence29 spectral samples and a fitted mean, while42 diagonal channel variances are estimated. Test one bounded estimator extension: shrink noisy DC diagonal variances toward a shared template and include finite-training-mean uncertainty. This is a statistical candidate, not evidence of a new instrument component. Cores may be dependent, so nominal sample count is an assumption rather than effective degrees of freedom.

Freeze before access: use per-mode power model's non-DC fixed choices radius0, lowalpha.6/middle.3/high.1, refitted on fitting western values. Let S be DC residual second moment diagonal (denominator n). Targets T are either constant mean(S), or the fitting-low-band diagonal covariance scaled to the same trace as S. Candidate variance vectors are D=(1-w)S+wT, w=[0,.25,.5,.75,1], followed by Gaussian predictive variance correction (n+1)/(n-1). This combines unbiased sample variance n/(n-1) and the plug-in mean uncertainty1+1/n. It is not an exact finite-sample predictive distribution or calibrated iid significance statement. Keep original uncorrected diagonal S as an explicit baseline. D remains diagonal; no new off-diagonal DC fit. Positive floors follow the inherited covariance regularizer. Eleven declared candidates: baseline first, then constant-target weights in ascending order, then low-band-template weights.

Three western folds choose candidate by mean DC Gaussian log density per channel, equal cores, first tie. Mean/channel templates are fit only on fitting cores. Refit all candidates on all west; save parameters, matrices and choice before eastern member read. No eastern retuning. Selected model and baseline receive full joint/aperture scores; all DC candidates receive per-core DC scores so failures cannot disappear.

Predeclared diagnostics: DC channel q individually; quartiles of the ORIGINAL all-west uncorrected DC variances (fixed channel groups), and quartiles ordered by the selected new variances. Report fixed-group metrics even if channel ordering changes. Also retain all prior non-DC halves/channel quartiles, individual spatial-mode q and adjacent cross-mode products for selected model. Same descriptive range[.8,1.2], no significance claim. Full likelihood admission remains blocked if subgroups fail or source/cross-mode behavior unvalidated.

Before data: analytic n/(n-1)*(1+1/n) correction identity, covariance positivity/trace/scale, exact Gaussian inverse/logdet, protected fitting-only interfaces and inherited orthonormal-transform controls. Algebra1e-10. Independent manual cosine-basis/DC/template refit and held score replay after execution. This is the only new branch; no response-dependent expansion.
