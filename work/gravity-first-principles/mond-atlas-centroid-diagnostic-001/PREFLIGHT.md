# Prospective diagnostic of retained centroid failures

Status SOURCE_BLOCKED for observational scoring. Source-conditioned predictions only. Execution032 is immutable. Investigate exactly the42 retained failed centroid comparison rows; recompute their distinct physical/nuisance cases on all four previously bound source quadratures, without fitting or changing source, motion, channel response or continuum coefficients.

For nonnegative stored-channel line profile p, define c_positive=sum(v*p)/sum(p). This is a centroid of the **captured window**, not total intrinsic line centroid. Report capture=sum(p)*abs(dv)/(1000*sum(aperture-weighted valid-emitter Jy km/s)). Also report finite63parent-channel capture. Invalid-motion emission remains outside this conditional prediction and is not silently assigned zero physical emission. The precontinuum profile is not a positive clipping of a signed spectrum: it is the known forward model before the continuum subtraction operator.

For signed profile s=A H f retain original c_signed=sum(v*s)/sum(s), cancellation kappa=sum(abs(s))/abs(sum(s)), signed-integral/positive-integral and both old refinement numbers. The exact difference identity is ca-cb=sum((a-b)*(v-cb))/sum(a); this exposes denominator sensitivity without changing any old gate. Report this identity and its triangle upper bound. Compare positive centroid difference against the original0.5km/s scale descriptively; no retrospective replacement or admission follows. ProfileL1 remains original signed metric.

Before source arrays: construct H independently for identity/fullHanning/decimatedHanning; test nonnegative unit row sums and equality with established operator; compare scalar Gaussian bin quadrature with CDF integral; symmetric positive centroid and cancellation counterexample. Require Gaussian bin error<1e-12, old saved prediction replay maxabsolute<1e-10mJy/beam, centroid-difference identity error<1e-8km/s. Finite/nonnegative storedprofiles, centroid within stored velocity range and capture between0 and1+1e-12 are implementation checks. Retain failures. Summaries include all42 rows and all fourcase outputs, not a selected best branch. No further formula/kernel/threshold search.
