# Prospective all192-case extension

The42-row diagnosis suggests a positive precontinuum centroid is a meaningful captured-window moment. It does not establish its convergence elsewhere. Before new arrays, freeze an extension to **all192 pre-existing nuisance cases**, allfourcaches, all15apertures and the samefive refinement pairs:14400comparisons. No case selection, parameter change, source adjustment, new mask or response access. Compute the same positive captured-window centroid, signed centroid, stored/parent capture and signedprofileL1. Compare positive centroid difference against0.5km/s over the full design; retain all failures and old signed gate separately. The old42 failures remain failures in their original protocol regardless of these results.

The independent H/Gaussian evaluator is unchanged algebraically. CUDAfloat64 accelerates the same CDF bins, positive linear mixing and continuum application. Compare GPU against independent CPU for one case in each spectralbranch before the fullsource calculation (maxabsolute bothpositiveandsigned<1e-10mJy/beam). Replay each saved signed spectrum to1e-10; positivecentroids must lie inwindow, capture in[0,1+1e-12]. Save all192case/fivepair rows, momentsallfourcaches and extrema. This is a prospective numerical diagnostic on previously specified predictions, not fresh observational evidence and not permission to waive source/instrument requirements. Bound runtime10minutes and no persistent newfullspectralcube; publicCSV diagnostics only.
