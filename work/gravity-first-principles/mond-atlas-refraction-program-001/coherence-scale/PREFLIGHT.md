# Finite coherence-scale permittivity: new physical prescription

Freeze two alternatives before their solves: epsilon depends on Gaussian-smoothed density with physical isotropic standard deviation ell=.25 or.5kpc. Epsilon remains `.2+.8*rho_s/(rho_s+1e7)`. The original unsmoothed rho remains on the RHS of div(epsilon gradPhi)=4piG rho. These are new nonlocal density-response prescriptions, not a numerical repair of the original point-density law. No parameter is fitted to a force target or observation.

Motivation is sensitivity to unresolved density gradients, which is not yet established as the root cause of the original failure. Use the same primary f4-stars-h0p4 source, conversions, component masses, isolation boundary and sampling. Gaussian kernel truncated at4 standard deviations and normalized on the infinite discrete grid; outside-domain density zero. Do not renormalize the finite-domain smoothed field. Report smoothing mass lost at the boundary and require less than0.1% of grid source mass. Separately report full source, all grid cells, interior RHS mass and excluded boundary-node mass.

For each ell, solve at prior fine(.125,.125,.0625) and finer(.0625,.0625,.03125) resolutions within half-widths(8,8,4). Also repeat base and enlarged-box grids from the initial protocol for each new law; the original point-law boundary check is not inherited. Same5% overall/8% per-height force thresholds, physical PDE relative residual<1e-8 and flux balance<1e-8. Preserve every failed case with no smoothing-length changes after seeing outcomes. Save sampled vectors/diagnostics only, no new raw fields. Eight solves, one CPU thread,180second iterative deadline for this branch.

Before actual sources: Gaussian positivity, constant-field preservation sufficiently far from padded boundaries, centered impulse mass conservation, and independent direct1D kernel convolution agreement at1e-12. Positive epsilon bounds are checked. Original solver and manufactured equation controls are unchanged/bound. Finite smoothing does not supply physical energy exchange, observed coherence phases or a metric, and no observed-response admission is gained.
