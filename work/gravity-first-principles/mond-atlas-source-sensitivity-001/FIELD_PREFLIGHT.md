# Source-alternative field replay frozen after source construction, before fields

Bind all source packets and independent source review. Select common30 q1
primary and stellarheight.4, missingzero primary and missingannular primary.
The q2 common30 sources provide the separate source-projection discretization
check, not an independently validated full gravity grid. All gravitational
parameters match Execution031. AlternativeHI weights its own column force.

Evaluate R=.05..6.025 step.025, azimuth1024/2048, exact target/source vertical
average. Newton96kpc grids.0625/.03125; log32kpc grids.03125/.015625 with
vertical128. Same1%RMS/3%point coarse/fine force and angular gates, each
component and total; full range and .75..2.5 reported separately. Do not fill
undefined zero-HI rings. This is a conditional force replay; no source-to-spectra
numerical admission claimed from this radial pilot, whose interpolation and
expanded smoothed-emission tails require their own full adapter checks.

Save each component and total, raw alternativeHI mean columns for pressure,
radial-force variation and tangential RMS. HI pressure and emission are required
to use the same alternative source; export an explicit contract, not baseline
pressure/emission relabeled as alternatives. Retain smoothed source tails beyond
R6 in packet and mass audit; do not cut or renormalize them to fit old support.
Compare against frozen original primary force table only at exact common radii;
these are source sensitivity differences, not observations or confidence limits.
No response cubes/spectra accessed. Source-only runtime300s/RAM16GB maximum.
