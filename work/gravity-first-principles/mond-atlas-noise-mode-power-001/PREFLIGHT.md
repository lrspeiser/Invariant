# Positive per-mode power with band-specific channel covariance

Execution027 background-only development test on the same hash-bound29western/27eastern NGC2976 cores. No new raw, source/emission pixels, motion/lensing targets or gravity scores. Both regions are historically exposed. Preserve the previous broad-band failures. One bounded model extension only; no follow-on adaptive branch.

Orthonormal type-II24x24 spatial DCT; same four bands: DC r²0, low1..9, middle10..64, high>64. Subtract fitting-west channel mean. Each spatial mode's raw power is mean squared coefficient over fitting cores and42channels. Pool this nonnegative power over modes in the same band within Euclidean frequency-grid radius R=[0,1,2], including self. Pool is arithmetic mean; no periodic boundary and no cross-band pooling. Enforce floor max(1e-300,1e-12 times median positive raw power in its band); no modes excluded. Normalize pooled powers to mean1 within band to fix covariance-factor scale degeneracy.

Divide each mode's spectral coefficients by sqrt(normalized power). Estimate band42channel second moment from all normalized fitting coefficients, shrink toward channel diagonal with alpha=[.1,.3,.6,1]. Twelve candidates per band R-major then alpha order. Three inherited geometry folds choose each band by held-west equal-core Gaussian log density per channel/mode INCLUDING log(power) term. Refit all candidates on allwest, save every power vector/covariance and choices before east. No east-based selection or floor change.

Evaluate selected model only on east: full joint and band q/N/log density, exact six aligned-aperture marginal covariances sum_m (A U_m)^2 power_m C_band, all per-mode q, prior lower/upper frequency-half and channel-eigenquartile diagnostics. Same descriptive q range[.8,1.2]; report counts and failed groups, not independence-based significance. Within each spatial band, halve at median mode r²; channels ordered by band covariance eigenvalue and split into4 groups. Report every mode; no filtering weak/high-frequency modes.

Cross-mode diagnostic: all adjacent horizontal/vertical DCT-grid mode pairs, including band boundaries, using normalized dot product of full whitened core/channel residual vectors. Report every pair, mean absolute value and count above abs .1 descriptively. Pair dependence, nonzero means and correlations not captured by scalar product remain; no calibrated significance claim. Report raw/pooled mode-power range, floor hits and ratio to double-precision epsilon times the largest power; these diagnose numerical dynamic range, not instrument noise origin.

Pre-data controls: Parseval and explicit complete-basis covariance/inverse/logdet; SPD and unit scaling; pooling positivity/locality and no mode loss; rank regularization. Algebra1e-10. Independent manual-basis source/power/matrix replay and inverse/aperture scores after run. CPU1thread; compact coefficients. Passing aggregate scores does not establish cross-mode independence or source/cross-core likelihood admission.
