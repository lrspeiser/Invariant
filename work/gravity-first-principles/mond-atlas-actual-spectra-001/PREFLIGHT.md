# Actual source conditioned spectrum refinement and callback

SOURCE_BLOCKED for observed scoring pending this adapter/source refinement. No observed source-region spectra or residuals may be read here. Use frozen NGC2976 supplied HI mass-centroid source, final column-force run004 table and validated sampled native emitter renderer. Read builder/solver admission policy v1. Primary source THINGS/Walter et al.2008 https://arxiv.org/abs/0810.2125; source map/CO/stellar dependencies retain their registered measurement assumptions. All data remain historically development-exposed; no independent discovery claim.

Freeze four projected emissivity quadratures: planar spacing.0625/.03125kpc crossed with exponential vertical Laguerre order24/48 per side (h=.2kpc). Use exact mass-centroid and positive cell flux from source-audit; stream actual3D projected nodes, then sum sampled-beam-weighted Jy km/s into each planar velocity group. No midplane replacement; projection includes every retained z node. Bind source packets and projection code before evaluation. All15 original12x12 apertures and42 native channels retained.

Force and column denominator use PCHIP within the final run004 radial table. No linear interpolation or extrapolation. Pressure Pi=s_ref²*Sigma_smoothed_HI uses the previous positive source-only Gaussian.25kpc reflected radial estimator; its analytic derivative is integrated with the same frozen raw radial table/Simpson rule. Rotation squared=R*g_column+R*s_ref²*Sigma_smoothed_prime/Sigma_raw. HI sensitivity scales pressure and raw denominator together and HI emission once; stellar/CO material factors act only through force. Test baseline material at both stellar heights(.1/.4), Newton/Newton+fixedlog, s_ref5/10/15, three spectral branches, both spin signs. LOS=spin*sin(inclination)*cos(phi)*sqrt(vphi²), plus a global systemic offset; the opposite signs are prescribed before observations. Supplied column rotation is constant with height and steady axisymmetric closure is conditional, not a general dynamical solution.

Keep every source group with negative rotation squared or outside force table as an unknown-velocity contribution. Never clamp vphi², assign an invented velocity, or call the global model steady. Render only supported nonnegative-vphi² groups and return a separate per-aperture absolute spectral envelope for all omitted positive emissivity, using the independently proved1000*maxrow sum|AH|/pregrid_dv coefficient. Apply the requested global multiplier to both rendered flux and envelope. The prior envelope1.1e-5mJy at multiplier2 is a prior result, not assumed valid for the new quadrature: recompute and report any change.

Callback parameters are global systemic velocity[-30,30]km/s, intrinsic sigma[3,20]km/s and emission multiplier[.5,2]. Enforce finite bounded inputs, no source or branch fitting in callback. Cache only source geometry/pressure/force and valid-node grouped flux; spectra remain exactCDF transforms. Expose fixed material choices for later separately authorized sensitivities, but no selection here.

Compare all actual source predictions at the narrowest allowed intrinsic sigma3km/s and at10km/s, with systemic0 and multiplier1. This is not a claimed uniform proof over the entire nuisance box. Multipliers cancel relative-refinement metrics. Separately test each callback's finite output at systemic +/-30 with sigma3 and20 without adapting source quadrature. Compare planar-only, vertical-only and joint refinement against the same signed predictions: each aperture relative profile L1<1%, centroid absolute change<.5km/s. Retain every failure and small-denominator metric. Full source integrated-flux closure1e-8 and exact grouped-versus-expanded manufactured controls required. All spin/branch failures remain visible; no response-based mask or nuisance changes.

Independent arithmetic replay must validate saved spectral comparisons and separately reconstruct at least representative actual predictions/unknown bounds from cached source groups and independent channel quadrature/continuum algebra. Report code/data hashes, all invalid-node statuses, source projection degeneracies, mask/noise limitations and source-resolution alternatives still missing. No observed motion scores, no calibrated joint likelihood, no commits from this worker. Derived large arrays private; public source/receipts/compact reports only.
