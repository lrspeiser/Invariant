Transform to ICRS retained differing frame attributes, so spherical_offsets_to still rejected frame equivalence. Run004 directly transforms into the explicitly defined catalog-origin SkyOffsetFrame, rather than relying on frame metadata equality. Original script retained. No cube response values or motion scores.
