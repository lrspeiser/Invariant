# Independent unknown-spectrum envelope

The parent's bound is valid for an arbitrary nonnegative line profile with normalized integrated flux, including arbitrary unknown velocity or intrinsic width. Let p_i be its probability mass in pre-grid bin i, of width d. Then0<=p_i<=1 and sum p_i<=1; mass outside the pre-grid only reduces the represented sum. The bin-averaged spectral density is p_i/d. The signed delivered spectrum is Bp/d with B=A H, where A includes continuum subtraction. Therefore each channel obeys |(Bp)_j|/d <= sum_i |B_ji|/d. Multiplying by nonnegative beam-weighted integrated flux and1000 gives the reported mJy/beam bound. Triangle inequality also permits any collection of emitters with different unknown line profiles.

The same proof applies to all three declared spectral alternatives. The decimated Hanning pre-grid has half the native bin width, so its conservative coefficient is doubled; ignoring this would understate that branch's bound. The continuum matrix has negative entries, which are handled by absolute values. The argument assumes the supplied nonnegative integrated emissivity and fixed spatial response; it does not cover an unspecified absorption/opacity change, source mass change or different instrument operator.

Independent index-coordinate least squares reconstructs A, and separately constructed boxcar/Hanning matrices reproduce all three coefficients. The42-channel outputs retain the actual63-parent grid. All252 reported maxima across84 cases are checked from their supplied positive aperture weights. The largest envelope is about5.52468e-6mJy/beam at emission multiplier1, and twice that at the allowed multiplier2. The HI sensitivity is already applied once to each supplied aperture-weighted flux; the emitter script multiplies by its HI factor once, while stellar/CO factors alter force feasibility rather than directly rescaling HI emission. No extra helium or Jy/beam conversion belongs in the bound.

A tighter but unnecessary mathematical bound follows from the full constraint sum p_i<=1: max_i|B_ji|/d. Its coefficients are recorded as an independent cross-check, without replacing the parent's deliberately conservative row-sum envelope or changing any admission threshold.

This review independently verifies the operator inequality and numerical envelope arithmetic. It inspects the code's HI scaling and verifies nonnegative original flux; it does not independently reconstruct the84 invalid-node masks or their beam-weighted sums. Those supplied weights and source support retain their existing validation obligations. All84 global steady solutions remain invalid. A small aperture-level unknown-contribution envelope does not establish a global steady state, invent missing velocities, or create an observational comparison.
