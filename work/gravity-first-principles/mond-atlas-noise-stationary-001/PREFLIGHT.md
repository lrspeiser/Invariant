# Stationary positive-kernel within-core noise model

BACKGROUND VALIDATION ONLY; no source/emission likelihood admission. Same hash-bound NGC2976 saved 29 west and 27 east background cores (24x24 pixels, 42 channels), three inherited geometry folds. Both sides historically exposed. No raw download, emission pixels, velocity targets or gravity scoring. Primary observation https://arxiv.org/abs/0810.2125; this covariance is phenomenological, not a measured instrumental beam model.

Freeze before reading arrays: retain channel-constant mean and channel covariance C estimated from fitting western cores, with fixed .3 diagonal shrinkage. Whiten channels using C. Estimate stationary lag products over fitting cores, channels and valid pixel pairs for displacements dy>=0, dx=-cap..cap, retaining dx>=0 at dy0 to avoid opposite-direction duplicates. Covariance estimator keeps residual core offsets; no local mean removal.

Spatial K(d)=w_white 1[d=0]+sum_j w_j exp[-|d|^2/(2 ell_j^2)], widths ell=[.5,1,2,4,8] native pixels. All coefficients nonnegative. Fit six coefficients by NNLS to lag covariances with square-root valid-spatial-pair-count weighting. This is a deterministic moment objective, not a claim of independent pairs. Enforce minimum white coefficient floor_fraction times fitting lag-zero variance. Nine candidates: lag caps[4,8,12] crossed with floor fractions[1e-6,1e-4,.01]. Subtract the fixed white floor before NNLS and add it back. No other regularization or coefficient tuning. Positive Gaussian kernels plus positive floor guarantee a finite-grid SPD covariance. No periodic wrapping.

Select candidate by equal-core joint Gaussian log density per voxel on three western held folds, declaration order breaks ties. Refit every candidate to all west, save coefficients, C and selection before eastern array access. Then report all nine joint scores and each of six exact aperture marginals (sides1,2,4,8,12,24), per-core scores, trace ratios, white amplitude and coefficient boundaries. Descriptive q/N range[.8,1.2] unchanged. No eastern retuning or parameter expansion.

Before arrays: explicit small Kronecker inverse/logdet, Gaussian-mixture positive definiteness and stationarity, unit/amplitude scaling, exact planted covariance coefficient fit, and valid lag indexing with explicit loops. Algebra1e-10, planted normalized residual1e-10. Independent chosen-model reconstruction and inverse-score/aperture replay after run. One CPU thread. Model applies within cores; cross-core covariance, nonstationarity, channel/spatial separability, mask purity and source-region transfer remain unvalidated.
