# Continuous nonclock development comparison

POST_HOC_DEVELOPMENT_ONLY; radial empirical laws, not admitted 3D opacity or energy-transfer physics. Original SPARC source definitions/cohort from configs/mond_atlas_clock_relay_v1.json and parent load_sources. Same 102 eligible historical development galaxies, 2212 eligible radii expected, three five-fold galaxy splits. No reserved bodies or halo quantities. Primary source paper https://arxiv.org/abs/1606.09251. Prior results are exposed; no independent confirmation claim.

Freeze before this branch scores responses: MF linear [.8,2]. MOND adjusted a0factor logarithmic [.1,3]. Coherence g=gb[1+A/(1+(Sigma/Sigma0)^n)] with A linear[0,100], Sigma0 logarithmic[.1,1e4], n=1 or free linear[.25,4]. Absorption/reemission g=gb[exp(-tau)+eta(1-exp(-tau))], tau=k Sigma/100, k linear[0,10], eta passive[0,1] or active[0,30]. Sigma=.5 MF max(SBdisk,0) is a stellar surface proxy, not intervening gas opacity. Active gain has no energy-conservation claim.

Fit globally within training galaxies using scipy least_squares and residual weights 1/sqrt(number_training_galaxies * rows_in_galaxy). Three deterministic starts per fold: normalized bound coordinates .15,.5,.85 for all parameters (log coordinates for declared positive scales). Maximum 1500 function evaluations; ftol,xtol,gtol=1e-9. Retain every result/failure. Only successful finite solutions compete. Explicit nested Newton model is fit with the same MF bound and three starts and allowed for coherence and relay families; ties favor Newton. Fixed MOND MF1/a0factor1 is separate. Family selections use training loss only; no parameter-bound expansion after scores.

Before response access: finite/zero-strength/eta1 Newton recovery, passive bound, independently written transfer formula, planted deterministic parameter recovery and changing held labels cannot alter training solution. Numerical tests threshold logspeed1e-10 for algebra, planted training MSE1e-12. Save source/code hashes, tests log, every fit/start, selected parameters, every held radial prediction, per-galaxy errors, signed mean log residuals inner r/Rd<1, middle1..3, outer>=3 with equal galaxy weighting and actual counts. Compare free-n vs n1 and trained MOND. Report optimizer convergence and boundary incidence. No new 3D, temporal, energy or lensing claims.
