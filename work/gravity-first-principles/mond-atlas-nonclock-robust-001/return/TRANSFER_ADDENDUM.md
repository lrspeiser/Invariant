# Frozen before evaluation: population transfer

In addition to three-seed five-fold evaluation, fit each family on the gas-rich
proxy group and predict the stellar-rich proxy group, then reverse. Group is
fixed from nominal sources: 1.33*MHI/(1.33*MHI+0.5*L3.6)>=0.5 is gas-rich.
Use the same 3 starts, bounds, zero candidates and equal-galaxy objective.
Report each direction separately, with all starts, chosen parameters, held
predictions and per-galaxy metrics. No recipient response enters fitting.

Finite_flat_bridge is a rational cored potential/force shape that overlaps a
generalized cored clock family. Freeing the outer transition C*rM is a shape
repair/control, not an independent physical mechanism. Radial speeds cannot
distinguish labels placed on the same potential.
