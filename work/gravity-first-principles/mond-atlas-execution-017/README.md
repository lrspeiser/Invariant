# Execution 017: hold the observations fixed, refine the source

The new GPU experiment shows that coarse source representation caused much of
NGC2976's thin-sheet stellar mismatch: 5.23% becomes 0.081% with finer latent
nodes, using identical measured cells and weights. CO reaches the 9.23% floor
imposed by negative signed data. All 36 fits converge. This separates source
representation and measurement limits from claims about gravity.

See [the complete source report](../mond-atlas-source-resolution-001/README.md)
and [the plot](../mond-atlas-source-resolution-001/findings-001/source-resolution.png).
Eight independent benchmark methods pass before source access. All 36 packets
are rehashed and their fixed weights, CPU projections and stationary optimality
receipts replay. All 864 prior manifest entries matched before the two mutable
handoffs were archived and updated. No unchanged broad suite was rerun solely
to inflate the test count; execution-016's 179-test evidence remains intact.

The separately created pressure and light-propagation tasks are still running
under frozen theory-only scopes. Their unfinished files are excluded. The
parent goal remains active: source likelihood, observed gas noise/selection,
lensing instrument calibration, more galaxies and group/survey transfer remain.
ZERO observed full-field cube or lensing likelihoods are admitted. No new
physical gravity formula, measured depth, or source superresolution is claimed.

Publication includes intended scripts, config, tests, summaries, plot and
receipts. Large arrays remain private. The cumulative manifest excludes itself;
its exact bytes and all listed files are verified against the Git index before
ordinary publication to main. Post-commit remote equality is recorded privately.
