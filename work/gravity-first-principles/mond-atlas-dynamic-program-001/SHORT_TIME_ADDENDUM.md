# Post-result diagnostic, frozen before rerun

All four clustered configurations failed long-time trajectory agreement despite
small invariant drift. To separate immediate numerical failure from accumulated
sensitivity, rerun exactly those equations and initial states to T=2 only, with
unchanged two resolutions and 401 output samples. Same final position normalized
threshold1e-3 and invariant drift1e-5. No changed softening or model parameters.
This diagnostic cannot establish mathematical chaos or validate long-time paths.
