{"status":"AUDIT_RULE_CORRECTED","reason":"Initial blanket finite check rejected source_mean NaNs in unobserved cells. These are excluded by zero fit and evaluation weights and are not the intrinsic source fed to gravity. Revised readback records every count and requires both exclusion weights zero; all actual source/operator inputs must remain finite. Parent source and field code unchanged."}
