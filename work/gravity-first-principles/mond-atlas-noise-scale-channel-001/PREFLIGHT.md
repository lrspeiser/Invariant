# Spatial-frequency-dependent channel covariance

Single bounded post-hoc development branch; preserve stationary and Kronecker failures. BACKGROUND ONLY, no emission/velocity/gravity arrays. Same NGC2976 saved29west/27east cores, same three geometry folds, previous exposure acknowledged. Primary observation https://arxiv.org/abs/0810.2125. This is a phenomenological finite-core orthonormal DCT model, not an instrument beam measurement.

Subtract fitting-west channel constant mean, transform each24x24 image with orthonormal type-II DCT. Spatial frequency bands: DC kx²+ky²=0; low1..9; middle10..64; high>64. Each band's42channel second moment is estimated by pooling fitting-west cores and all modes in that band. Modes are assumed mutually independent, and each band shares one covariance; this explicitly drops cross-mode covariance and within-band variance variation. The full basis plus positive diagonal-shrunk channel matrices defines SPD joint covariance.

For each band, choose shrinkage [.1,.3,.6,1] toward its own channel diagonal using only geometry-defined western three-fold mean log density per channel/mode, equal cores. Fixed mean is refit on each fitting subset. DC rank29<42 is regularized, not claimed fully measured. Refit all candidates on all west; save means, matrices, band counts and choices before eastern array read. Evaluate selected combination plus four uniform-shrink compositions to retain comparisons without enumerating256 choices. All selected choices are western-only; no east retuning or further adaptive branch.

Joint q/N and log density sum every independent spatial mode/channel. Derive exact aligned aperture covariance sum_m (A U_m)^2 C_band(m), for all positions and sides1,2,4,8,12,24; score marginal q and trace with equal cores, retain every position/core summary. Same descriptive q range[.8,1.2]. No independence-based confidence intervals, no cross-core covariance or source-likelihood admission.

Before actual arrays: Parseval/orthonormal basis, explicit small full covariance/inverse/logdet, aperture projection identity, positive covariance rank regularization. Algebra tolerance1e-10. Bind original packet hash and geometry. Independent inverse joint-score and aperture replay after run. CPU1thread, compact4x42² matrices per candidate, no new raw bytes. Passing aggregate jointq cannot certify mode correlations, channel likelihood shape or emission masks.
