# TWELL-400 source-shaped rebind and replay task brief

Status: **UNSTARTED IMPLEMENTATION — exact gap mapped from current frozen artifacts**

This brief closes the difference between the frozen target-free operator packet and an honest source-shaped replay. It does not authorize response access or scoring.

## Frozen inputs

- `runs/gravity/twell-400-v2-typed-compiler-final-v3/cards.jsonl`
  - 400 exact formula cards: 380 atomic and 20 compound.
  - 1,184 exact parameter cells; all pass target-free operator probes.
  - All 400 remain `REGISTERED_THEORY_ONLY_PENDING_FINAL_SOURCE_MATRIX`.
- `runs/gravity/open-gravity-static-radial-adapter-v1/receipt.json`
  - 126 provisional TWELL bindings: 120 atomic plus compounds X01, X05, X10, X13, X17 and X18.
  - Adapter mechanics pass synthetically, but `twell_rebind.status=DEFERRED_NOT_REBOUND` and the bindings have no campaign authority.

## Exact compatibility gap

The current adapter supports architectures A01–A14 and A19 and source drivers D01–D07 and D13. That yields 15 × 8 = 120 atomic bindings plus six compounds.

The remaining 274 cards split cleanly and without overlap:

1. **84 temporal-architecture cards**: all 21 cards in each of A15_RETARDED, A16_MEMORY, A17_RESONANCE and A18_STOCHASTIC. These require typed time histories, initial conditions, cadence and causal execution rather than a static-radial substitute.
2. **190 missing-driver cards on otherwise supported architectures**:
   - 180 atomic cards: 15 supported architectures × the 12 unsupported drivers D08–D12 and D14–D20.
   - 10 compounds: X02, X03, X04, X06, X07, X08, X09, X14, X16 and X20.

The six presently adapted compounds are X01, X05, X10, X13, X17 and X18. The four temporal compounds inside the 84-card group are X11, X12, X15 and X19.

## Required successor artifacts

Create append-only successors; never rewrite the frozen compiler or adapter.

1. A source-availability matrix with one row for every `(formula_id, domain, source_release)` and exact dispositions `EXECUTABLE`, `SOURCE_BLOCKED`, `UNADAPTED`, `INCOMPATIBLE_FEATURE_SET`, `THEORY_ONLY` or `NUMERICAL_INVALID`.
2. Execution bindings for every honestly supported formula. Each binding must name the exact callable, parameters, required source features, geometry/time capability, emitted observables, units, limits and resource ceiling.
3. Temporal adapters for A15–A18 only where a source-shaped time history exists. Static replication or label multiplication is forbidden.
4. Driver implementations D08–D12 and D14–D20 only where the source packet contains the declared primitive or a hash-bound response-blind derivation. Missing drivers remain explicit blocks.
5. A replay matrix over every executable card, every frozen parameter cell, every compatible source-shaped scenario and every registered noise/nuisance family. Retain all invalid and blocked cells.
6. An append-only result ledger linking compiler card SHA, formula SHA, adapter SHA, source release SHA, scenario/truth/seed/nuisance identity, prediction SHA and metric SHA.

## Mandatory gates

- Exact 400/400 card identity, order, card SHA and formula SHA agreement with the v3 stream.
- Exact 1,184/1,184 parameter-cell coverage: every cell is completed or explicitly blocked; no collapsed binding-level placeholder may stand for multiple parameter cells.
- Formula inputs are limited to declared response-blind feature packets. Derived features inherit source/response taint recursively.
- Units, ordered axes, coordinate frame, geometry support, time support, boundary/initial conditions, finiteness and output shape are checked at execution.
- All same-formula deterministic replays are byte-identical; rotations/translations/units commute where the card claims them.
- Exact equivalence families are scored once and recorded as ties; finite-fixture similarity is not promoted to formula identity.
- Synthetic results use `SYNTHETIC_DIRECTIONAL_SIGNAL` only. No empirical support/rejection or publication claim follows from this replay.
- A distinct agent must independently regenerate the compatibility counts and a representative full execution slice before the successor is admitted.

## Current evidence statement

The defensible statement today is: **400 executable target-free operator cards exist, but only 126 have provisional static-radial source adapters and none of those bindings has completed the final source-matrix rebind.** The remaining 274 are now accounted for exactly rather than hidden in an aggregate count.
