{
  "id": "mond-atlas-smoothing-null-v1",
  "admission_disposition": "SOURCE_BLOCKED",
  "scope": "Conditional stationary-Gaussian reference distributions for the already observed center/outer MAD ratios; no new galaxy motion or gravity score.",
  "native_summary": "work/gravity-first-principles/mond-atlas-native-spectral-001/summary.json",
  "preprocessing_protocol": "configs/mond_atlas_preprocessing_replay_v2.json",
  "preprocessing_summary": "work/gravity-first-principles/mond-atlas-preprocessing-replay-002/summary.json",
  "replicates_per_case": 256,
  "batch_replicates": 8,
  "seed": 9062040,
  "spatial_surrogates": ["independent_native_pixels", "circular_native_major_beam_filter"],
  "spatial_contract": "Use the actual native dimensions and pixel size. Apply the previously replayed extra Gaussian and 8x8 block average as a finite linear operator. The second surrogate first filters independent native pixels with a circular Gaussian whose FWHM is the recorded native major beam. This is a sensitivity model, not a measured interferometer covariance or a recovered dirty beam.",
  "spectral_surrogates": ["independent_parent_channels", "hanning_parent_channels"],
  "spectral_contract": "Propagate each surrogate parent spectral covariance through the recovered polynomial continuum-subtraction operator A C A^T, then restrict to the same retained historical continuum-fit candidate channels. Normalize marginal channel variances because each channel MAD ratio is invariant to scale. No actual visibility covariance is claimed.",
  "sampler": "Sample the separable Gaussian directly on the coarse grid using exact finite-operator spatial covariance and selected-channel covariance. Verify against the native linear transformation on small grids and bound float32-rounding differences. This avoids generating discarded native pixels for every replicate.",
  "statistics": ["Per-galaxy median of channel center/outer MAD ratios", "Mean log of those galaxy-level ratios across all eight galaxies"],
  "interval_quantiles": [0.025, 0.5, 0.975],
  "tail_summary": "Report (1 + number of reference draws at least as large as the observed statistic)/(replicates+1). This is an exploratory conditional reference fraction, not a calibrated observational or gravity p-value. Preserve all four surrogate branches.",
  "benchmarks": {
    "finite_operator_direct_relative_rms_max": 1e-6,
    "separable_covariance_dense_relative_error_max": 1e-12,
    "sample_covariance_relative_rms_max": 0.04,
    "sample_covariance_draws": 30000
  },
  "exposure": "All observed ratios were examined before this protocol. This diagnoses a development-data issue and is not independent confirmation.",
  "nonclaims": ["Gaussian stationary surrogates do not reconstruct the actual visibility sampling or nonstationary noise", "A tail discrepancy does not identify faint gas, continuum residuals or other instrumental structure as its cause", "Spatial-surrogate variation is not an observational uncertainty posterior", "No selection mask or full cube likelihood is admitted by this experiment"]
}
